# 验证报告：Q2「能否直接复用数据源」结论复核

> 日期：2026-09-22
> 分支：feature/phase2.2-track
> 周期：185e0601b262447c91ca9f3d96717ebc
> 对象：E004 祝工
> 范围：仅验证，未做任何修复

---

## 结论先行

我上一条 Q2 报告的「展示侧是纯 wiring、零模型改动」是不准确的——它漏掉了「每行总分本身就取错了」这一层。截图里的 2.65 正是周期级 composite，被错误地填进了每个项目行的 total。

---

## Q1. `getCalibrationMatrix` 每行的 originalScore/adjustedScore 取的是小计还是 composite？

**取的是周期级 composite，不是项目任务小计。**

证据在 `CalibrationService.java`：

```java
// 127-153 行：外层遍历 results（每个员工一条 AssessmentResult），
//             内层遍历 resolveGroups（每个 project|stage 一个 GroupRef）
for (AssessmentResult result : results) {
    for (GroupRef ref : resolveGroups(...)) {
        ...
        row.setOriginalScore(result.getOriginalScore());   // 136 行
        row.setAdjustedScore(result.getAdjustedScore());   // 137 行
        ...
    }
}
```

- `result` 是 `AssessmentResult`，主键 `(period_id, assessee_id)`，**没有 project 列**，一条员工记录只存一个周期级总分。
- `resolveGroups`（345-373 行）把这一条 result **展开**成多行（多项目/多阶段各一行），但每一行都塞进**同一个** `result.getOriginalScore()`/`getAdjustedScore()`。
- 所以多项目员工 N 行 = N 个 `GroupRef`，共享同一个周期 composite 值。**这就是截图里祝工两行都显示 2.65 的直接原因。**

---

## Q2. 「展示侧是纯 wiring、零模型改动」是否成立？

**不成立。遗漏了「每行总分本身取错」这一层。**

我原报告说「只需把 `resolveIndicators` 的结果塞进 `Row.kpis`」，隐含假设是「行的 total 已经是对的，只缺逐 KPI 明细」。实际是：**行的 total 也是错的**——它读的是周期 composite，不是该项目任务的小计。

所以即便补上 `kpis`，行首的「原始总分/调整后总分」仍然会两条都显示 2.65，而不是 P006 的 1.0、P007 的 4.0。修复不只是一处 wiring：`getCalibrationMatrix` 建行时，`originalScore`/`adjustedScore` 必须从 `assessment_score`（该项目任务的单项分）按 `ScoreCalculator.weightedSum(score, 归一化weight)` 重算，而不是从 `assessment_result` 直接取。

- 「零模型改动」（不需要改 DB schema）这点**仍成立**——`assessment_score` 已经存了逐 KPI 分，重算小计不需要新列。
- 但「纯 wiring」**不成立**：这是**改取数口径**，不是加字段。

---

## Q3. 原报告是否明确识别出「周期 composite 与项目任务小计是两层」？

**概念上识别了，但没把它落到当前 bug 上。**

我在原报告 Q4 的「关键澄清」里确实写了：「项目任务加权小计…与周期 composite(finalScore)是两层，PD 重算的是该项目任务小计，不必反推周期 composite」。这层是对的。

但我在 Q2 又用「纯 wiring、零模型改动」框定展示侧，等于**默认行的 total 没问题**——这与我已识别的「两层」自相矛盾。

正确的诊断应该是：**当前 `Row.originalScore/adjustedScore` 取的是错误的那一层（composite），需要改成小计那一层。**

---

## Q4. 截图 vs DB 对照：2.65 的来源（已确认）

DB 实测（dev 库，E004 祝工，周期 `185e0601…`）：

| 项 | DB 值 |
|---|---|
| `assessment_result.original_score` | **2.6500** |
| `assessment_result.adjusted_score` | **2.6500**（version=0，无 score_adjustment 审计行 → 从未被真正「调整」） |
| task 142 P006-P2 | kpi39=1.0、kpi46=1.0（权重各 1.0） |
| task 141 P007-P2 | kpi39=4.0 |
| task 143 FUNCTIONAL | 工作质量=3.0 |
| 参与比重 | P006=50%、P007=50% |
| 岗位配置 | AI技术类/AI产品岗，project_weight=0.7、func_weight=0.3 |

2.65 的精确反推（与 `ResultService.computeComposite` + `ScoreCalculator` 完全吻合）：

```
P006 小计 = weightedSum([1,1], 归一化[0.5,0.5]) = 1.0
P007 小计 = 4.0
项目 composite = 0.5×1.0 + 0.5×4.0 = 2.5
职能 composite = 3.0
finalScore = 0.7×2.5 + 0.3×3.0 = 1.75 + 0.9 = 2.65   ← 精确命中
```

**所以 2.65 = `assessment_result.original_score` = 周期级 composite（finalScore），既不是 P006 的 1.0，也不是 P007 的 4.0。** 截图两行都显示 2.65，就是因为两行读同一条 result 的同一个 composite。

### 附带发现（需在实现时定口径，不是需要回抛的决策）

你说任务详情弹窗「P006 加权总分 2.00」，但弹窗（`TaskListPage.jsx:129-136`）**只显示 指标名称/权重/得分 三列，没有总分列**；且权重 1.0 被渲染成 `100%`（两个指标各 100%，和为 200%）。你的「2.00」是 `1×1.0 + 1×1.0 = 2.0` 的**未归一化** Σ(score×weight)。

而代码算 composite 时 `averageTaskScores`（`ResultService.java:343-345`）把权重**归一化到和=1**，所以 P006 的「正确」小计是 **1.0，不是 2.0**。

即：改分目标落到「项目任务小计」时，要明确用哪个口径（归一化后 1.0，还是弹窗视觉上的 2.0），否则 PD 改完单项、界面重算的小计会和周期 composite 的组件值对不上。

---

## 判定

- **「能否复用数据源」—— 能复用**（`assessment_score` + `resolveIndicators` 同源，这部分对）。
- **「展示侧纯 wiring、零模型改动」—— 错**，漏了「每行总分取错层（composite 当小计）」这个必须先修的点。

## 状态

`DONE` — 根因已复核并锁定，未修复（按指示仅验证）。

真正待实现的改动从「加 kpis」升级为两步：

1. 行 total 改为按项目任务重算小计（不再读 `assessment_result` 的 composite）。
2. 再补逐 KPI 明细。

写路径仍是我此前说的落点缺口：`assessment_score` 无校准覆盖字段、`assessment_result` 无项目小计列。
