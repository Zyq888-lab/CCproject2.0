# 绩效考核系统 · 开发进度报告

**报告日期**：2026-09-21　**当前分支**：`feature/phase2.2-track`　**最新提交**：`48c9075`

---

## 一、总览

继峰座椅绩效考核系统的建设目标是**让系统自动完成数据汇总和算分，HR 从「数据搬运工」变成「流程监督者」**。项目按四阶段推进，目前已交付至 Phase 2.2，进入「流程闭环」的完整形态。

| 阶段 | 名称 | 核心目标 | 状态 | 交付时间 |
|------|------|----------|:----:|----------|
| Phase 1 | 配置中心 | HR 独立完成所有考核规则配置 | ✅ 已完成 | 2026-05-29 |
| Phase 2.0 | 打分闭环 | 参与申报 → 审批 → 打分 → 通知 | ✅ 已完成 | 2026-08-20 |
| Phase 2.1 | 校准确认 + 角色主标记 | PD 校准 → 总裁确认 → 结果查询 | ✅ 已完成 | 2026-09-17 |
| Phase 2.2 | 按项目总裁确认 + 一键激活/强制改密 | 逐项目确认 + 结果发布 + 账号激活改密 | ✅ 已完成 | 2026-09-21 |
| Phase 3 | 增强功能 | 报表导出 / 申诉 / 邮件 / 归档 | 🔲 未启动 | — |

---

## 二、Phase 1 — 配置中心（✅ 已完成）

**5/18 ~ 5/29，28 项任务全部完成**，让 HR 不依赖开发人员即可配置全部考核规则。数据模型 V1~V9。

- **员工信息管理**：列表/搜索/筛选/增删改/分页，直属上级单个+批量设置
- **项目角色管理**：角色卡片增删停用，删除时引用检查保护（岗位/KPI 引用拦截）
- **项目管理**：项目增删、PM 确认阶段、ADMIN 强制重置、乐观锁并发保护
- **项目角色分配 + 汇总视图**：按项目指定角色人员、主 PD 标记、跨项目汇总 + 5 项筛选 + CSV 导出
- **岗位考核配置 / 岗位分类**：项目/职能权重（合计 100% 校验）、考核人角色、分类增删保护
- **KPI 指标库**：项目 KPI（角色×阶段）+ 职能 KPI（分类×岗位）双标签页、实时权重提示、启停
- **考核周期管理**：创建/开始/关闭、活跃唯一性校验
- **仪表盘**：8 张配置引导卡片 + 完成度进度条
- **批量导入**：员工 Excel 模板下载/预览/导入/结果报告
- **用户与权限**：6 角色（ADMIN/总裁/PD/PM/评估人/员工）、多角色分配、菜单权限隔离、登录安全（CSRF + bcrypt）

**质量**：19 个后端测试类全通过、137 条 UAT 用例、端到端 23 步 21 通过（健康分 93/100）。

---

## 三、Phase 2.0 — 打分闭环（✅ 已完成）

**8/12 ~ 8/20，T1~T12 + 增量**，端到端跑通「发起考核 → 打分提交」。数据模型 V10~V15。

| 模块 | 功能 |
|------|------|
| 项目参与 | 员工填报多项目 + 投入比重（合计 100% 实时校验）、PM 审批（通过/不通过+建议比重）、被拒重新提交 |
| 考核任务 | launch 批量生成 + 审批增量生成、6 态状态机、开始/取消、Savepoint 容错 + DB 幂等 |
| 考核打分 | 项目/职能 KPI 评分（1~5 滑块）、草稿暂存、凭证上传（≤10MB）、提交锁定 |
| 通知 | 站内消息写表、未读计数（铃铛红点）、已读标记、`@Async` 不阻塞 API |
| 差异报告 | 缺配置员工跳过 + `discrepancy_log` 持久化 + 查询端点 |
| 仪表盘升级 | 待处理计数（按角色）、配置进度、差异报告 |
| 周期监控 | 全员任务流转矩阵 + 筛选 + CSV 导出 |

**质量**：`mvn test` 180 pass、前端 `npm run build` 3112 modules、Playwright E2E 2 场景。

---

## 四、Phase 2.1 — 校准确认 + 角色主标记（✅ 已完成）

**8/20 ~ 9/17**，两条线并行。数据模型 V20~V25。

**线 A — 角色主标记**（修「同角色多人重复收任务」的路由错位）：
- `is_primary_pd → is_primary` 字段泛化改名（V20，含手写 SQL、公开 API 参数、建项目写入点同步）
- `resolvePrimaryAssessors` 收敛 launch + 审批增量两条任务生成路径：恰一个主只发主、多人无主跳过+记 `NO_PRIMARY_ASSESSOR` 差异+通知 admin
- 前端「标记主」按钮语义泛化（任意角色可标主）

**线 B — 校准确认**（把考核从「停在 SUBMITTED」推进到「出结果」）：
- 结果落库 `assessment_result`（原始分 + 调整分 + 乐观锁）+ `score_adjustment` 审计（V22）
- composite 加权总分计算（单组件员工权重重归一化）
- 周期状态机扩展：`INIT→ONGOING→CALIBRATING→CONFIRMED→COMPLETED`
- **PD 校准矩阵**：分布汇总带 + 离群（±1σ）优先排序 + 行内改分（原因必填 + 原分 ghost 差额）
- **总裁确认页**：汇总 + 按钮 + 强保护二次确认（输入周期名）
- **员工结果页**：结果分 = 调整分，并排原始分 + 差额 + 原因 + KPI 明细
- 改分后通知评估人、移除退回死代码（V23）

---

## 五、Phase 2.2 — 按项目总裁确认 + 一键激活/强制改密（✅ 已完成）

**9/17 ~ 9/21**，含 Track A + Track B 两轨及收尾。数据模型 V26~V28。这是最近一期开发，全部内容已于 9/21 提交（commit `48c9075`）。

**Track A — 用户一键激活 + 强制改密**：
- 一键激活：账号 = 员工工号、初始密码 = `123456`（`DEFAULT_INITIAL_PASSWORD` 可配）、`must_change_password=true`
- 强制改密硬门 `MustChangePasswordFilter`（后端 403，非仅前端跳转）、密码强度校验（≥8 位 + 字母数字 + 非默认）、改密后注销会话
- 前端：登录跳改密页、`ChangePasswordPage`、用户管理改「激活账号」

**Track B — 按项目总裁确认 + 发布**：
- `project_confirmation` 逐项目确认表（V27，`PENDING/APPROVED/RETURNED` + 退回次数上限 `PRESIDENT_RETURN_TIMES=3`）
- 负责总裁解析 `resolvePrimaryPresident`（项目角色分配 PRESIDENT 标主），总裁**数据隔离**只看自己负责项目
- 逐项目确认通过 / 退回；全部通过后原子翻转周期 `CALIBRATING→CONFIRMED`
- **周期状态机第 6 态 `PUBLISHED`**：`CONFIRMED→PUBLISHED`（ADMIN 发布）→ 结果才对员工可见（`isResultVisible` 对 `PUBLISHED/COMPLETED` 才返回 true）
- PD 对退回项目重校准后走 resubmit（`RETURNED→PENDING`）；未分配总裁项目记 `NO_PRESIDENT` 差异卡住周期

**收尾 — 校准确认流程四项补强（本次会话）**：
1. **人员级退回**：校准矩阵回填 `confirmationStatus/returnReason`（V28 加 assessee_id），被退回人员单独可改分、其余只读，退回意见展示在行内
2. **总裁可见性与 drill-down**：总裁矩阵只看自己作为主总裁的项目
3. **结果发布可见性**：结果页白名单补 `PUBLISHED`（此前只认 CONFIRMED/COMPLETED，已发布结果员工却看不到）
4. **待处理跳转按角色**：总裁→总裁确认 / PD→考核校准 / 评估人→考核任务 / 员工·PM→项目参与 / ADMIN→项目列表

---

## 六、Phase 3 — 增强功能（🔲 未启动）

已登记的设计/待办（`docs/designs/phase3-president-approval.md` 及 TODOS.md）：报表导出、申诉管理、邮件通知通道、历史数据归档、对象存储凭证渲染、总裁审批「纯职能员工」口径开放问题。

---

## 七、已知技术债 / 待办（TODOS.md）

| 优先级 | 事项 |
|:--:|------|
| P2 | 参与录入审批由谁批（是否走「主 PM」） |
| P3 | 对象存储接入 + 凭证真实渲染（当前 uploadEvidence 只写占位 URL） |
| P3 | 逐行 N+1 债务扫除（MyAssessmentService 等） |
| P3 | 设计系统 DESIGN.md 缺失 |
| P3 | 校准矩阵离群分组键与标签口径不一致 |
| P3 | `averageTaskScores` 空分任务按 0 计压低均值 |
| P3 | `generateNextUserId` 并发撞号 |
| P3 | abort 强制关闭后结果对员工可见（需独立 `ABORTED` 终态） |

---

*本报告基于仓库实际提交历史与设计文档整理，反映截至 2026-09-21 的真实进度。Phase 1 ~ Phase 2.2 已全部完成，Phase 3 未启动。*
