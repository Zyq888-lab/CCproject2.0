# Phase 1 待清理清单

> 扫描日期: 2026-08-12 | 未修改任何文件

---

## 一、SetupWizard — 明确可删

### 前端 (9 个文件)

| 文件 | 状态 |
|------|:--:|
| `pages/SetupWizard/SetupWizardPage.jsx` | ❌ 无路由引用 |
| `pages/SetupWizard/StepAssignRole.jsx` | ❌ 无路由引用 |
| `pages/SetupWizard/StepCreatePeriod.jsx` | ❌ 无路由引用 |
| `pages/SetupWizard/StepCreateProject.jsx` | ❌ 无路由引用 |
| `pages/SetupWizard/StepImportEmployee.jsx` | ❌ 无路由引用 |
| `pages/SetupWizard/StepKpiConfig.jsx` | ❌ 无路由引用 |
| `pages/SetupWizard/StepPositionConfig.jsx` | ❌ 无路由引用 |
| `pages/SetupWizard/StepProjectRole.jsx` | ❌ 无路由引用 |

**证据**: `App.jsx` 未 import 也未注册任何 `/setup-wizard` 路由；`AppLayout.jsx` 侧边栏无向导菜单项；`DashboardPage.jsx` 无向导入口链接。

### 后端 (5 个源文件 + 2 个测试文件)

| 文件 | 被引用位置 | 状态 |
|------|----------|:--:|
| `wizard/WizardController.java` | 仅自身 + test | ❌ 无外部引用 |
| `wizard/WizardService.java` | WizardController + test | ❌ 无外部引用 |
| `wizard/WizardProgress.java` (实体) | WizardService + Mapper | ❌ 无外部引用 |
| `wizard/WizardProgressDTO.java` | WizardService + WizardController | ❌ 无外部引用 |
| `wizard/WizardProgressMapper.java` | WizardService | ❌ 无外部引用 |
| `test/.../wizard/WizardControllerTest.java` (18 tests) | — | 随源码一起删 |
| `test/.../wizard/WizardServiceTest.java` (9 tests) | — | 随源码一起删 |

**证据**: 全项目 grep `import.*wizard` 仅命中 wizard 包自身 + 测试。无 Controller/Service/Job/Listener 引用。

### DB 表

`wizard_progress` 表（`V8__create_wizard_progress.sql`）— 仅 wizard 包使用，可随源码删除。

### 删除步骤

```bash
# 前端
rm -r frontend/src/pages/SetupWizard/

# 后端
rm -r src/main/java/com/jifeng/assessment/wizard/
rm -r src/test/java/com/jifeng/assessment/wizard/
rm src/main/resources/db/migration/V8__create_wizard_progress.sql
```

---

## 二、LeaderConfig — 明确可删

### 前端 (1 个文件)

| 文件 | 状态 |
|------|:--:|
| `pages/LeaderConfig/LeaderConfigPage.jsx` | ❌ 未被 import |

**证据**: `App.jsx:43` 路由 `/leader-config` 直接 redirect 到 `/employee-management`，未 import LeaderConfigPage。

### 后端 — 保留

`EmployeeController` 有 `PUT /batch-update-leader` 端点用于批量修改直属上级，该功能已整合到 `EmployeeListPage` 的批量编辑中。后端无需清理。

### 删除步骤

```bash
rm -r frontend/src/pages/LeaderConfig/
```

同时删除 `App.jsx:43` 的 redirect 路由：
```js
// 可删
<Route path="/leader-config" element={<Navigate to="/employee-management" replace />} />
```

---

## 三、pom.xml — 无不明确依赖

所有依赖均有用途：

| 依赖 | 用途 | 判定 |
|------|------|:--:|
| spring-boot-starter-web | REST 接口 | ✅ |
| spring-boot-starter-security | 认证/鉴权 | ✅ |
| spring-boot-starter-aop | Controller AOP 日志 | ✅ |
| spring-boot-starter-validation | @Valid/@NotBlank | ✅ |
| mybatis-plus | ORM | ✅ |
| flyway-core + postgresql | 数据库迁移 | ✅ |
| postgresql | 数据库驱动 | ✅ |
| testcontainers + postgresql | 测试 (test scope) | ✅ |
| lombok | 代码生成 | ✅ |
| poi-ooxml | Excel 导入(ImportService) | ✅ |
| spring-boot-starter-test | 测试框架 | ✅ |
| spring-security-test | 测试安全 | ✅ |

**结论**: 无冗余依赖。

---

## 四、package.json — 无不明确依赖

| 依赖 | 用途 | 判定 |
|------|------|:--:|
| react / react-dom | 框架 | ✅ |
| antd / @ant-design/icons | UI 组件库 | ✅ |
| axios | HTTP 客户端 | ✅ |
| react-router-dom | 路由 | ✅ |
| xlsx | Excel 解析(客户端导入) | ✅ |
| vite / @vitejs/plugin-react | 构建工具 | ✅ |
| @playwright/test | E2E 测试 | ✅ |
| eslint + plugins | 代码检查 | ✅ |

**结论**: 无冗余依赖。

---

## 五、其他检查项 — 均正常

| 检查项 | 结果 |
|--------|:--:|
| `PositionCategoryPage` 是否独立存在 | ❌ 不存在，已嵌入 PositionConfigPage |
| `EmployeeImportModal` | ✅ 被 EmployeeListPage import |
| `ProjectRolePage` | ✅ 被 ProjectRoleView import |
| `FuncKpiPage` / `ProjectKpiPage` | ✅ 被 KpiConfigView import |

---

## 总结

| 类型 | 文件数 | 影响 |
|------|:--:|------|
| 前端可删 | 9 | SetupWizard(8) + LeaderConfig(1) |
| 后端源码可删 | 5 | wizard 包全部 |
| 测试可删 | 2 | WizardControllerTest + WizardServiceTest |
| DB迁移可删 | 1 | V8__create_wizard_progress.sql |
| App.jsx 路由清理 | 1 行 | leader-config redirect |
| **合计** | **18** | 无功能影响 |

全部删除后不影响任何现有功能。SetupWizard 是配置向导（Phase 1 早期设计，后来被独立页面替代），LeaderConfig 功能已合并到员工管理的批量编辑中。
