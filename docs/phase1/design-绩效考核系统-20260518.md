# Design: 项目制绩效考核系统 — 架构设计与实施路线

Generated by /office-hours on 2026-05-18
Branch: master
Repo: Zyq888-lab/CCproject2.0
Status: CEO_REVIEWED
Mode: Intrapreneurship (内部项目)

## Problem Statement

继峰座椅公司需要一套项目制绩效考核系统。现有流程半系统半手工：HR/OA系统能管基础数据，但不支持多项目权重分配、动态KPI指标库、PD校准等核心逻辑。HR每个考核周期需要2-3周手工汇总Excel数据、套公式算分——繁琐且易出错。事业部总裁/HR VP已决策要求建立系统化考核体系。

核心痛点：**现有系统不支持\"一个员工参与多个项目，按投入比重分配考核权重\"的计算模型**，这是项目制考核区别于传统职能考核的关键。

## Demand Evidence

- 领导层（事业部总裁/HR VP）直接决策推动，不是技术部门的自发项目
- HR部门是最大痛点承受者——从数据搬运工变成流程监督者是最直接的ROI
- 现有半系统半手工流程的瓶颈明确：多项目权重计算、校准流程、全局汇总视图

## Status Quo

现有HR/OA系统能做基础数据管理，但以下环节靠手工：
- 员工项目参与申报（Excel填写，邮件审批）
- 多项目权重分配与校验（HR手工计算）
- KPI指标库管理（无统一标准，各项目自行定义）
- 考核打分汇总（Excel收集，HR手工套公式）
- PD校准和总裁确认（邮件+线下会议）

## Target User & Narrowest Wedge

**核心用户**：HR/系统管理员（2-3人）——当前流程中工作量最大、效率最低的角色。

**最窄楔入点**：配置中心。先让HR能在系统里管理所有考核规则（员工、角色、KPI、岗位配置），这是整个考核体系的地基。配置中心本身就有独立价值——即使考核流程暂时还靠手工，HR也能用系统统一管理标准。

## Glossary — 关键术语精确定义

PRD中有几个容易混淆的概念，在此明确定义：

| 术语 | 精确定义 | 对应PRD中的模糊点 |
|------|----------|-------------------|
| **用户角色** | 系统的登录权限角色：ADMIN、总裁、PD、PM、评估人、员工。一个账号可有多角色 | PRD称为"用户角色" |
| **项目角色** | 员工在具体项目中的职能角色：PDL、PQL、Launch等。动态可配置，非权限角色 | PRD称为"项目角色"，但与用户角色易混 |
| **项目考核人** | 审批员工项目参与申报的人。由系统根据岗位配置中的 `project_assessor_roles` 自动从项目角色分配表中匹配 | PRD中US-601使用此术语 |
| **评估人** | 对员工进行KPI打分的人。项目考核中评估人=项目考核人（同一人既审批参与又打分）；职能考核中评估人=直属上级 | PRD中两个概念有重叠 |
| **考核人** | 评估人的同义词，在考核任务上下文中使用 | — |
| **项目阶段** | P2=项目启动/设计阶段、P3=产品开发/验证阶段、P4=工艺验证/试生产阶段、P5=量产阶段 | PRD 4.1.3有定义 |

## Constraints

- 面向200-500人规模
- 阶段1并发用户≥5（2-3个HR同时操作），阶段2并发≥50，阶段3并发≥100
- 项目角色必须动态可配置（刚需，非过度设计）
- 审批层级需灵活设计：阶段2做"是否需总裁确认"开关，阶段3做完整可配置审批流
- 阶段1不做通知（HR自己操作无需通知）；阶段2做基本站内消息（审批流依赖）；阶段3做邮件通知
- 阶段1仅桌面布局；阶段2起Web响应式适配手机（员工需在手机上申报和查分）
- 数据需符合PIPL（个人信息保护法）基本要求：访问审计、敏感数据加密存储

## Security & Access Control

**认证策略**：Phase 1使用 Spring Security + Session-based 登录（用户名+bcrypt(12)密码哈希），启用 CSRF 保护（AJAX通过Header传递CSRF token）。Phase 2起评估对接继峰现有SSO/LDAP，届时只替换认证Provider，授权层不变。

**密码存储**：bcrypt(12) 哈希，不可逆。`user.password_hash` 字段存储 bcrypt 输出（60字符）。

**RBAC模型**：六种用户角色（ADMIN、总裁、PD、PM、评估人、员工），权限矩阵见PRD第五章。核心原则：
- 员工仅可查看本人数据
- PM仅可操作所负责项目的数据
- PD仅可查看本人关联项目的数据（通过项目角色分配中PD角色+负责人标记确定）
- 所有关键操作写入审计日志（`audit_log`表）

**API鉴权**：所有 `/api/*` 接口检查登录态+角色权限，敏感操作（删除、退回、确认）二次校验。

**行级数据权限**：使用 Spring Security `@PreAuthorize` + SpEL 表达式实施。涉及员工数据的接口，普通员工角色自动过滤为本人数据（`#employeeId == authentication.principal.employeeId`），ADMIN/总裁不受此限制。防止通过修改URL中ID参数访问他人数据（IDOR攻击）。

## API Design Principles

- 协议：REST over HTTPS，JSON请求/响应
- 版本：URL路径版本 `/api/v1/...`
- 错误格式：`{ "error": { "code": "VALIDATION_ERROR", "message": "...", "details": [...] } }`
- 分页：列表接口统一 `?page=1&size=20`，响应含 `total`、`page`、`size`
- 并发控制：所有配置表和项目表的更新接口使用乐观锁（`version` 字段），冲突时返回 409 Conflict + 提示"数据已被他人修改，请刷新后重试"
- 阶段1优先实现基础数据接口（员工CRUD、项目角色CRUD、项目CRUD、配置CRUD）

## Premises

1. ✅ 项目角色动态可配置 — 刚需。继峰组织架构调整频繁，新角色会持续出现
2. ✅ 审批层级需灵活设计 — 支持可配置的审批环节，系统适配制度而非反过来
3. ✅ 职能考核按岗位配置决定 — "是否纳入项目制"开关控制考核方式
4. ✅ MVP只做站内消息 — 降低初期复杂度，邮件后续版本加

## PRD 问题诊断

原始PRD（V1.2，1152行）存在以下结构性问题：

| 问题 | 影响 |
|------|------|
| "是什么"和"怎么实现"混在一起 | 页面布局、接口设计甚至邮件模板与核心业务逻辑混排，难以快速定位关键决策点 |
| 核心流程优先级不清晰 | 22项P0功能平铺，缺少分层——配置、流程、辅助功能混在一起 |
| 概念体系有冗余 | "用户角色"和"项目角色"两套体系、"项目考核人"和"评估人"关系需反复对照 |
| 缺少现状对比 | 未描述现有流程的痛点和差距，难以判断每个功能的实际ROI |

## Recommended Architecture: 三阶段递进

### 阶段1：配置中心（MVP）

**目标**：HR能独立完成所有考核规则的配置和管理。

```
配置中心
├─ 员工管理 CRUD（含邮箱、直属上级、岗位、状态；阶段1物理删除）
├─ 项目角色管理（动态增删改查，含引用检查）
├─ 项目管理 + 阶段确认（PM确认→乐观锁锁定→ADMIN可强制重置）
├─ 岗位考核配置（权重、考核人角色映射[关联表]、是否纳入项目制；保存时校验角色引用）
├─ 项目KPI指标库（角色 × 阶段 × 指标，权重校验）
├─ 职能KPI指标库（按岗位，权重校验）
├─ 系统参数配置（凭证必填、退回次数上限等）
├─ 考核周期管理（创建/编辑/关闭）
├─ 配置向导（7步引导：项目角色→导入员工→创建项目→分配角色→配置KPI→配置岗位→创建周期）
└─ 空状态设计规范（所有列表/表格页统一引导式空状态）
```

**不做的**：任何考核流程（申报、打分、校准、确认、申诉）——这些在阶段2。

**交付物**：一个HR可以独立使用的配置中心，覆盖PRD中的数据基础部分。

### 阶段2：考核流程闭环

**目标**：端到端跑通一次完整考核。

```
考核流程
├─ 发起考核（ADMIN触发，生成考核周期+任务，站内消息通知评估人）
├─ 项目参与申报（员工填写→项目考核人审批，待审批项在仪表盘显示）
├─ 考核打分（项目KPI评分+职能KPI评分，支持凭证上传）
├─ PD校准（汇总视图，退回/修改分数）
├─ 总裁确认（汇总视图，确认/退回PD；支持ADMIN配置跳过此环节）
└─ 结果查询（员工查看本人得分+明细+凭证）
```

**阶段2通知机制**：以内联触发方式实现（写 `notification` 表 + 仪表盘红点，无独立通知中心页面）——审批流的前提是审批人需要知道有待处理项。Phase 3升级为通用通知服务（独立通知中心UI + 邮件通道 + 可扩展的通知类型）。

阶段2内联通知触发点：
- 发起考核 → 通知所有评估人
- PD提交 → 通知总裁（如启用）
- 退回重评 → 通知被退回的评估人
- 结果确认 → 通知员工

**核心设计决策**：
- 审批流可配置：ADMIN通过系统参数（`NEED_PRESIDENT_CONFIRM`）控制是否需要总裁确认环节。Phase 2开关实现，Phase 3升级为完整可配置审批流
- 考核关系自动生成：根据项目角色分配+岗位配置自动匹配，无需手动指定

### 阶段3：增强功能

```
增强
├─ 通知系统（站内消息，预留邮件通道）
├─ 申诉管理（员工发起→PD复核→总裁终裁）
├─ 催办（PM/PD手动触发，站内消息通知）
├─ 报表导出（Excel异步导出）
├─ 历史考核记录
└─ 审计日志 + 数据归档
```

## Data Model (Core)

核心实体关系，聚焦阶段1需要的表：

```
employee ──┬── employee_project_participation ──┬── project
            │                                    │
            ├── position_assessment_config       ├── project_role_assignment
            │   │ (岗位→考核权重/模式)             │   (项目→角色→人员)
            │   │                                │
            │   └── position_assessor_role_config├── project_role (动态可配置)
            │       (岗位→考核人角色，关联表)       │
            │                                    │
            └── func_kpi_config                  └── project_kpi_config
                (职能KPI指标)                        (角色×阶段×KPI)
```

关键设计点：
- `project_role` 是独立实体，不硬编码——角色代码+名称+描述+状态
- `position_assessor_role_config` 是岗位与考核人角色的关联表（`position_config_id → role_code`）。替代了逗号分隔字符串方案，支持索引查询"哪些岗位配置引用了角色PDL"
- `position_assessment_config` 中的考核人角色通过关联表获取，系统据此从 `project_role_assignment` 中自动匹配考核人
- 审批流节点信息存储在 `assessment_period` 或独立的 `approval_flow_config` 中

## 考核关系自动生成算法

**触发时机**：ADMIN点击「发起考核」时批量生成；后续项目参与记录审批通过后**同步触发**增量生成。

**触发条件**：项目参与记录状态变更为"审批通过"时立即触发，同步为该参与记录生成对应的考核任务。

**事务边界**（D5）：审批通过和考核任务生成在同一数据库事务中（`@Transactional`）。审批状态更新→任务生成→一起提交或一起回滚。若任务生成失败，审批也回滚，用户看到"审批失败，请重试"。

**匹配规则（伪代码）**：
```
对于每个考核周期内的项目参与记录（状态=审批通过）：
  1. 获取员工E的岗位配置C = position_assessment_config[E.position]
     若 C 不存在 → 记录到差异报告（缺岗位配置），跳过该员工
  2. 获取考核人角色列表 R = position_assessor_role_config[config_id=C.id]
     (如 ["PDL", "PQL"])
  3. 对于每个角色 r in R：
     a. 从 project_role_assignment 查找项目中担任角色r的人员列表
     b. 若找到 → 每个人员生成一条 PROJECT 类型的 assessment_task
     c. 若未找到 → 降级策略：
        - 使用 E.direct_leader 作为考核人（上级代考）
        - 若上级也为空 → 记录到差异报告（无考核人可指派），跳过该角色
  4. 生成一条 FUNCTIONAL 类型的 assessment_task：E.direct_leader 考核 E
     若 E.direct_leader 为空 → 记录到差异报告（无职能考核人）
```

**去重**：同一员工+同一项目+同一考核人只生成一条任务。

**差异报告**（D4）：批量生成完成后，系统汇总所有异常项（缺岗位配置/考核人缺失/上级为空），输出到ADMIN仪表盘。异常清单格式：

| 员工工号 | 姓名 | 项目 | 异常类型 | 详情 |
|----------|------|------|----------|------|
| EMP001 | 张三 | PRJ2025001 | 缺岗位配置 | 岗位"整椅研发岗"无考核配置 |
| EMP002 | 李四 | PRJ2025002 | 无考核人 | PDL角色在项目中无人担任，且无上级 |

ADMIN可选择：补配置后「一键补生成」该员工的任务，或「标记为不参与考核」。

## 考核任务状态机

```
PENDING ──评估人开始评分──→ IN_PROGRESS ──提交──→ SUBMITTED
   │                            │                    │
   │                            │                    ├── PD退回 ──→ RETURNED ──重新提交──→ SUBMITTED
   │                            │                    │
   │                            │                    └── PD确认 ──→ CONFIRMED
   │                            │
   └── 员工离职/项目取消 ──→ CANCELED
```

| 状态 | 含义 | 触发角色 | 允许操作 |
|------|------|----------|----------|
| PENDING | 待评分 | 系统生成 | 开始评分（→IN_PROGRESS）；ADMIN可取消 |
| IN_PROGRESS | 评分中（草稿） | 评估人 | 暂存草稿、提交（→SUBMITTED） |
| SUBMITTED | 已提交 | 评估人 | PD退回（→RETURNED）；PD确认/校准（→CONFIRMED）；评估人撤回（→IN_PROGRESS，仅PD未开始审核时） |
| RETURNED | 已退回 | PD | 评估人修改后重新提交（→SUBMITTED）；退回次数超限→标记争议 |
| CONFIRMED | 已确认 | PD/总裁 | 不可修改 |
| CANCELED | 已取消 | 系统/ADMIN | 不可恢复 |

## 最终得分计算公式

```
员工最终得分 = 项目考核加权得分 × 岗位项目权重 + 职能考核得分 × 岗位职能权重

其中：
  项目考核加权得分 = Σ(项目i的KPI加权得分 × 项目i的投入比重)
    - 项目i的KPI加权得分 = Σ(指标得分 × 指标权重)
    - 项目i的投入比重 = 员工在项目i的 participation_rate
    - 约束：Σ(所有项目投入比重) = 100%

  职能考核得分 = Σ(职能指标得分 × 职能指标权重)

计算示例（某员工参与2个项目，投入比重60%+40%，岗位项目权重70%，职能权重30%）：
  项目A得分 = 4.2, 项目B得分 = 3.8
  项目考核加权得分 = 4.2×0.6 + 3.8×0.4 = 4.04
  职能考核得分 = 4.0
  最终得分 = 4.04×0.7 + 4.0×0.3 = 3.828
```

## 权重校验规则

| 校验场景 | 规则 | 错误提示 |
|----------|------|----------|
| 项目KPI指标 | 同一 `项目角色 + 项目阶段` 下所有指标的权重之和 = 1.00 | "指标权重之和必须为100%" |
| 职能KPI指标 | 同一 `岗位分类 + 岗位名称` 下所有指标的权重之和 = 1.00 | "职能指标权重之和必须为100%" |
| 岗位考核配置 | `项目考核权重 + 职能考核权重 = 1.00` | "项目权重+职能权重必须为100%" |
| 员工投入比重 | 员工所有项目的 `投入比重` 之和 = 100% | "投入比重总和必须等于100%" |
| 单项投入比重 | 单个项目投入比重 ≥ 1% | "单个项目投入比重不能小于1%" |

## 汇总视图定义（PD/总裁）

**矩阵结构**：
- **行**：项目名称
- **列**：各职能角色（从项目角色列表中动态获取，含"项目均分"列）
- **单元格计算**：先计算每位成员的加权总分（Σ指标得分×指标权重），再对该职能下所有成员的加权总分取算术平均值
- **末列**（项目均分）：该项目下所有职能单元格值的算术平均

**高亮规则**：行内最高分标注 🟢，最低分标注 🔴

**交互**：点击任意单元格展开侧边抽屉，显示该职能下所有成员明细——姓名、各指标得分、加权总分、评估人、凭证状态

**查询策略**：动态列（项目角色）不适合数据库层CROSSTAB。采用**应用层装配**——先查询评分数据（按项目+角色+成员分组聚合），在应用层将角色值透视到列上。Phase 2数据集小（200人×10项目×5角色）时性能可控。Phase 3数据量增长后可引入物化视图（评分提交时刷新），将汇总结果预先计算存储。

## 周期中异常处理

考核周期进行中可能发生的变更场景：

| 场景 | 处理规则 |
|------|----------|
| 员工离职 | 已提交的评分保留；未完成的考核任务标记为 CANCELED；已确认结果保留 |
| 员工调岗 | 已生成的考核任务不回溯修改；岗位变更从下一考核周期生效 |
| 项目取消 | 该项目相关的考核任务标记为 CANCELED；员工的项目参与记录标记为作废 |
| 项目角色人员变更 | 已生成的考核任务不回溯；新增人员从变更时刻起参与评分；删除人员时检查是否已有评分 |
| 考核周期重叠 | 系统约束：同一时间只能有一个活跃周期（状态≠COMPLETED） |
| 阶段确认并发 | PM确认和ADMIN强制重置使用乐观锁（`version`字段），冲突时返回409 + "数据已被他人修改，请刷新后重试" |
| 配置编辑并发 | 所有配置表（岗位/KPI/角色分配）的更新使用乐观锁，后保存者收到冲突提示，不静默覆盖 |

## 数据迁移风险

从继峰现有HR/OA系统迁移数据的关键风险：

| 风险 | 缓解措施 |
|------|----------|
| 源系统无API，无法自动同步 | 阶段1内置Excel/CSV批量导入工具，含校验、去重、试运行预览 |
| 源数据格式不一致 | 导入模板定义标准列（员工ID、姓名、邮箱、岗位分类、岗位名称、部门、直属上级ID），映射由HR在导入时配置 |
| 历史考核数据迁移 | 历史数据不迁移到新系统。新系统上线前的周期数据由原系统/Excel归档保管 |
| 直属上级关系缺失 | 导入时校验必填字段；缺失数据在导入报告中高亮，HR手动补全 |

## Proposed Technical Stack

| 层 | 建议方案 | 备选 | 选择理由 |
|----|----------|------|----------|
| 前端 | React + Ant Design | Vue3 + Element Plus | Ant Design企业级组件丰富（表格、表单、权限），适合CRUD密集型管理后台 |
| 后端 | Spring Boot 3 + MyBatis-Plus | Django + DRF | 继峰大概率是Java技术栈；MyBatis-Plus对复杂查询和动态SQL友好 |
| 安全 | Spring Security + CSRF + bcrypt(12) | — | 阶段1建立安全基线；Phase 2对接SSO时只替换认证Provider |
| 数据库 | PostgreSQL 15 | MySQL 8.0 | 复杂聚合查询（汇总视图）、JSON字段（审计日志的old_value/new_value）是刚需 |
| 迁移 | Flyway | Liquibase | Spring Boot原生集成；所有DDL版本化为 V1__...sql, V2__...sql |
| 缓存 | Redis | — | Session存储 + 考核周期状态缓存 |
| 异步 | Spring @Async + 内存队列 | RabbitMQ | 阶段1-2无高吞吐异步需求；阶段3报表导出可升级为消息队列 |
| 日志 | SLF4J + Logback（JSON格式） | — | Controller AOP统一拦截记录（URL/用户/耗时/状态码），方便后续接入ELK |

> 此选型为建议方案，最终技术栈需与继峰IT部门确认——特别是数据库和部署环境的限制。

## Data Governance

- **数据分级**：考核分数和申诉内容为敏感数据（PIPL要求），需加密存储（AES-256 at rest）
- **访问审计**：所有查看、修改考核分数的操作记录到 `audit_log`（操作人、时间、操作类型、目标、变更前后值）
- **数据保留**：当前周期数据保留在主库；已结束且超过90天的周期自动归档到历史表（`_archive`后缀），归档后不可修改
- **数据导出**：员工仅可导出本人数据；管理员导出含敏感字段时需二次确认

## Testing Strategy

| 阶段 | 测试重点 | 方法 |
|------|----------|------|
| 阶段1 | CRUD完整性、权重校验规则（含浮点精度: 0.3+0.3+0.4）、角色引用检查、乐观锁并发冲突、批量导入边界（空文件、超大文件、重复ID） | 单元测试 + HR UAT（用真实数据导入验证） |
| 阶段2 | 考核关系生成准确性（含差异报告）、算分公式正确性（JUnit参数化测试 vs Excel公式结果，±0.001容差，≥10个测试用例覆盖单项目/多项目/边界权重/极端分数）、并发提交评分、审批流状态机 | 单元测试 + 集成测试 + 端到端流程UAT（选取1-2个项目试点） |
| 阶段3 | 通知送达率、异步导出、归档完整性、100并发负载 | 集成测试 + 负载测试 |

**算分交叉验证**（D11）：用Excel构造测试用例（含设计文档中的示例 4.2/3.8/4.0 → 3.828，以及极端值 1.0/5.0），Excel公式计算结果作为 expected value，JUnit参数化测试逐条比对。这是HR验收的核心信心来源。

## Open Questions

1. **审批流可配置的粒度**：Phase 2已决定用"是否需总裁确认"开关，Phase 3升级为完整可配置审批流。开关是否足够？还是需要在Phase 2就支持更灵活的节点配置？
2. **PD与项目的关联方式**：PRD中PD通过"项目角色=PD且标记负责人"关联项目。是否需要更直观的配置方式（如独立的"PD管辖范围"设置）？
3. **考核人自动匹配的降级策略**：PRD定义了"上级代考→标记待管理员指定"两级降级。实际运行中降级发生频率如何？如果频繁发生，是否需要更智能的匹配规则？
4. **阶段1的验收标准**：谁来验收配置中心？建议让继峰HR用真实数据走一遍完整配置流程（创建角色→导入员工→配置KPI→创建周期），验收通过标准：无需开发人员协助即可独立完成
5. **技术栈确认**：文档中建议了React+Spring Boot+PostgreSQL，需与继峰IT部门确认是否有技术限制（如必须用MySQL/必须用Vue等）

## Success Criteria

- **阶段1**：HR能在2小时内完成首次考核周期配置（含学习时间）。后续周期如使用前一周期模板复制（仅修改增量），应在30分钟内完成
- **阶段2**：一次完整考核周期（200人参与）从发起到结果确认，HR的手工介入时间从2-3周降到<2小时
- 系统可用性≥99.5%，页面加载<2秒，汇总视图矩阵加载<3秒

## Distribution Plan

- Web应用，浏览器访问
- 部署在继峰内网环境
- CI/CD流水线待定（后续版本确定具体方案）

## Dependencies

- 继峰现有员工数据（工号、姓名、岗位、部门、直属上级关系）——需确认数据来源和格式
- 项目阶段定义（P2-P5）需与继峰项目管理办公室对齐
- 无外部API依赖

## Next Steps

1. **确认Open Questions**：特别是审批流可配置的粒度和阶段1的验收标准
2. **数据迁移计划**：确认现有员工数据、岗位数据的来源和导入方式
3. **阶段1技术选型**：确定技术栈（前端框架、后端语言、数据库）
4. **搭建阶段1原型**：从员工管理和项目角色管理开始

---

# 附录

> 以下附录从PRD V1.2中浓缩提取，作为开发时的功能需求清单。若与正文架构决策冲突，以正文为准。

## 附录A: 功能清单（按阶段分配）

### 阶段1：配置中心（P0-配置部分）

| # | 功能 | 说明 | 来源 |
|---|------|------|------|
| A1 | 员工管理CRUD | 工号/姓名/邮箱(必填)/岗位分类/岗位名称/部门/直属上级/状态 | P0-01 |
| A2 | 用户角色分配 | ADMIN可将除ADMIN外的角色分配给任意账号；一个账号可多角色 | P0-02 |
| A3 | 项目角色管理 | 动态增删改查项目角色（PDL、PQL、Launch等）；被引用时不可删除 | P0-03 |
| A4 | 项目管理CRUD | 项目编码/名称/阶段(P2-P5)/状态；PM确认阶段→锁定；ADMIN可强制重置 | P0-04 |
| A5 | 项目角色分配 | 为项目配置角色人员；PD通过"角色=PD+标记负责人"关联项目 | P0-05 |
| A6 | 岗位考核配置 | 项目权重+职能权重=100%；指定项目考核人角色；指定默认项目角色 | P0-06 |
| A7 | 项目KPI指标库 | 角色×阶段×指标；权重之和=100%；支持停用/排序 | P0-07 |
| A8 | 职能KPI指标库 | 按岗位配置职能KPI；权重之和=100% | P0-08 |
| A9 | 职能关系配置 | 直属上级关系维护；支持批量导入Excel(≤10MB, ≤1000行) | P0-16 |
| A10 | 系统参数配置 | SCORE_EVIDENCE_REQUIRED / MAX_RETURN_TIMES / DEFAULT_PERIOD_DURATION_DAYS | P0-21 |
| A11 | 考核周期管理 | 手动创建/编辑/关闭周期；同一时间只能有一个活跃周期 | P0-22 |

### 阶段2：考核流程（P0-流程部分）

| # | 功能 | 说明 | 来源 |
|---|------|------|------|
| A12 | 发起考核 | ADMIN点击发起→自动生成考核周期+考核任务+通知评估人 | P0-17 |
| A13 | 项目参与录入与审批 | 员工填写多项目+投入比重(总和100%, 单项目≥1%)→项目考核人审批(通过/不通过+建议值)；项目考核人也可直接录入 | P0-09 |
| A14 | 项目考核打分 | 评估人按KPI指标1-5分评分(允许1位小数)；支持凭证上传；可暂存草稿；提交后不可修改 | P0-10 |
| A15 | 职能考核打分 | 直属上级按职能KPI指标评分 | P0-11 |
| A16 | PD校准 | 查看项目×职能汇总矩阵；可修改分数或退回评估人重评(最多N次，超出标记争议)；所有项目审核完成后统一提交总裁 | P0-12 |
| A17 | 总裁确认 | 查看全员汇总矩阵；确认通过或退回PD(不可越级)；确认后结果锁定+通知员工 | P0-13 |
| A18 | PD汇总视图 | 按项目×职能矩阵，🟢🔴高亮，点击展开成员明细 | P0-15 |
| A19 | 总裁汇总视图 | 同PD视图但范围全员 | P0-14 |
| A20 | 考核结果查询 | 员工查看本人得分/明细/凭证(总裁确认后才可查看)；PM/PD/总裁按权限查看 | P0-20 |
| A21 | 催办 | PM/PD手动催办→站内消息通知；催办记录可查 | P0-18 |

### 阶段3：增强功能（P0-申诉 + P1）

| # | 功能 | 说明 | 来源 |
|---|------|------|------|
| A22 | 申诉管理 | 员工发起→PD复核→总裁终裁；支持上传凭证；终裁后自动更新分数 | P0-19 |
| A23 | 登录认证 | Session方式登录 | P1-01 |
| A24 | 仪表盘 | 考核进度/平均分/待处理任务统计 | P1-02 |
| A25 | 通知系统 | 站内消息+邮件双通道；异步队列发送；重试3次 | P1-07 |
| A26 | 批量录入 | PM批量为多人录入项目参与信息 | P1-03 |
| A27 | 报表导出 | Excel异步导出(.xlsx)；完成后站内消息通知下载 | P1-05 |
| A28 | 数据隔离 | 员工仅看自己数据；PM/PD/总裁按权限范围查看 | P1-06 |
| A29 | 历史考核查看 | 按周期查询历史记录（从归档库路由） | P1-08 |
| A30 | 数据归档 | 周期结束后90天自动归档到_archive表 | P2-03 |
| A31 | 审计日志 | 关键操作记录（SUBMIT/RETURN/CALIBRATE/CONFIRM等） | P2-04 |

---

## 附录B: 核心数据表（关键字段）

### employee（员工表）— 阶段1
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| employee_id | String | 是 | 工号，PK |
| name | String | 是 | 姓名 |
| email | String | 是 | 邮箱（通知用） |
| category | String | 是 | 岗位分类（一类，如"研发技术类"） |
| position | String | 是 | 岗位名称（二类，如"整椅研发岗"） |
| org_name | String | 是 | 职能组织/部门 |
| direct_leader_id | String | 否 | 直属上级工号 |
| status | Enum | 是 | ACTIVE / INACTIVE |

### user（系统用户表）— 阶段1
| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | String | PK |
| username | String | 登录名 |
| password_hash | String | 密码哈希 |
| employee_id | String | FK→employee |

### user_role（用户角色分配表）— 阶段1
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | PK |
| user_id | String | FK→user |
| role_type | Enum | ADMIN / 总裁 / PD / PM / 评估人 / 员工 |

### project_role（项目角色表，动态可配置）— 阶段1
| 字段 | 类型 | 说明 |
|------|------|------|
| role_code | String | PK，如"PDL" |
| role_name | String | 如"项目研发负责人" |
| description | Text | 角色说明 |
| is_active | Boolean | 默认TRUE |

### project（项目表）— 阶段1
| 字段 | 类型 | 说明 |
|------|------|------|
| project_code | String | PK，如"PRJ2025001" |
| project_name | String | 项目中文名称 |
| project_stage | Enum | P2 / P3 / P4 / P5 |
| description | Text | 项目说明 |
| status | Enum | ACTIVE / INACTIVE |
| stage_confirmed | Boolean | PM是否已确认阶段 |
| confirmed_by | String | 确认人 |
| confirmed_at | DateTime | 确认时间 |
| version | Long | 乐观锁版本号（阶段确认/重置时检查） |

### project_role_assignment（项目角色分配表）— 阶段1
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | PK |
| project_code | String | FK→project |
| project_role_code | String | FK→project_role |
| employee_id | String | FK→employee |
| is_primary_pd | Boolean | 是否PD负责人 |
| version | Long | 乐观锁版本号 |

### position_assessment_config（岗位考核配置表）— 阶段1
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | PK |
| category | String | 岗位分类 |
| position | String | 岗位名称 |
| is_project_based | Boolean | 是否纳入项目制 |
| default_project_role | String | 默认项目角色 |
| func_assess_mode | Enum | DIRECT_LEADER / ORG_LEADER |
| project_weight | Decimal | 项目考核权重（默认0.70） |
| func_weight | Decimal | 职能考核权重（默认0.30） |
| version | Long | 乐观锁版本号 |

> 考核人角色映射已移至 `position_assessor_role_config` 关联表（规范化，D10）。

### position_assessor_role_config（岗位考核人角色关联表）— 阶段1 ✨新增
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | PK |
| position_config_id | Long | FK→position_assessment_config |
| role_code | String | FK→project_role，考核人角色代码 |
| version | Long | 乐观锁版本号 |

> 唯一约束：(position_config_id, role_code)。查询"引用角色PDL的所有岗位配置"使用 `JOIN` + 索引，替代 `LIKE '%PDL%'`。

### project_kpi_config（项目KPI指标配置表）— 阶段1
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | PK |
| project_role_code | String | FK→project_role |
| project_stage | Enum | P2 / P3 / P4 / P5 |
| kpi_name | String | 指标名称 |
| evaluation_criteria | Text | 1-5分评价标准 |
| weight | Decimal | 权重 |
| sort_order | Int | 排序 |
| is_active | Boolean | 默认TRUE |
| version | Long | 乐观锁版本号 |

### func_kpi_config（职能KPI指标配置表）— 阶段1
| 字段 | 类型 | 说明 |
|------|------|------|
| id | Long | PK |
| category | String | 岗位分类 |
| position | String | 岗位名称 |
| kpi_name | String | 指标名称 |
| evaluation_criteria | Text | 评价标准 |
| weight | Decimal | 权重 |
| sort_order | Int | 排序 |
| version | Long | 乐观锁版本号 |

### assessment_period（考核周期表）— 阶段1
| 字段 | 类型 | 说明 |
|------|------|------|
| period_id | String | PK |
| period_name | String | 如"2026年上半年考核" |
| start_date | Date | 开始日期 |
| end_date | Date | 结束日期 |
| status | Enum | INIT / ONGOING / CALIBRATING / COMPLETED |

### 以下表属于阶段2+

| # | 表名 | 简述 | 所属阶段 |
|---|------|------|----------|
| 12 | employee_project_participation | 员工项目参与表（含审批状态、投入比重） | 阶段2 |
| 13 | assessment_task | 考核任务表（考核人×被考核人配对，含状态和退回次数） | 阶段2 |
| 14 | assessment_score | 考核评分表（含KPI得分、凭证URL、状态） | 阶段2 |
| 15 | appeal | 申诉表（员工发起→PD复核→总裁终裁） | 阶段3 |
| 16 | notification | 通知表（站内消息） | 阶段2(内联) |
| 17 | audit_log | 审计日志表（操作人/时间/类型/目标/变更前后值JSON） | 阶段3 |
| 18 | system_param | 系统参数表（key-value） | 阶段1 |

---

## 附录C: 校验规则汇总

### C1 项目参与录入校验
| 校验项 | 规则 | 错误提示 |
|--------|------|----------|
| 比重总和 | 所有项目比重之和 = 100% | "投入比重总和必须等于100%" |
| 单项比重 | 单个项目比重 ≥ 1% | "单个项目投入比重不能小于1%" |
| 项目有效性 | project_code必须存在且状态=ACTIVE | "项目不存在" |
| 角色有效性 | project_role必须在project_role表中存在且is_active=TRUE | "项目角色不存在" |
| 考核人有效性 | 项目+角色必须有对应考核人 | "未找到该项目的考核人，请联系管理员" |

### C2 KPI指标配置校验
| 校验项 | 规则 | 错误提示 |
|--------|------|----------|
| 唯一性 | 项目角色+阶段+指标名称 唯一 | "该指标已存在" |
| 权重总和 | 同角色+同阶段下所有生效指标权重之和 = 1.00 | "指标权重之和必须为100%" |
| 单指标权重 | 单个指标权重 > 0 | "指标权重必须大于0" |
| 职能唯一性 | 岗位分类+岗位名称+指标名称 唯一 | "该职能指标已存在" |

### C3 考核打分校验
| 校验项 | 规则 | 错误提示 |
|--------|------|----------|
| 指标完整性 | 所有KPI指标必须打分 | "存在未评分的指标" |
| 得分范围 | 1分 ≤ 得分 ≤ 5分，允许1位小数 | "得分必须在1-5分之间" |
| 凭证必填 | SCORE_EVIDENCE_REQUIRED=TRUE时每个指标必须上传凭证 | "请上传评分凭证" |

### C4 流程校验
| 校验项 | 规则 | 错误提示 |
|--------|------|----------|
| PD提交条件 | 所有项目均审核完成 | "请完成所有项目校准后再提交" |
| 总裁退回 | 只能退回给PD，不可越级 | "不支持越级退回" |
| 结果查看 | 总裁确认后员工才可查看 | "考核结果尚未最终确认" |
| 阶段确认 | 发起考核前所有项目必须已确认阶段 | "项目{name}尚未确认阶段" |
| 退回次数 | 单个项目退回次数 ≤ MAX_RETURN_TIMES | "已超出退回次数上限" |

---

## 附录D: 页面清单

| # | 页面 | 路由 | 权限 | 所属阶段 |
|---|------|------|------|----------|
| 1 | 登录页 | /login | 公开（阶段2启用登录，阶段1内网直接访问） | 阶段1+ |
| 2 | 仪表盘 | /dashboard | ADMIN（阶段1：配置进度+差异报告提醒） | 阶段1 |
| 3 | 配置向导 | /setup-wizard | ADMIN（7步引导：角色→员工→项目→分配→KPI→岗位→周期） | 阶段1 |
| 4 | 员工列表 | /employee/list | ADMIN | 阶段1 |
| 5 | 用户角色分配 | /user-role | ADMIN | 阶段1 |
| 6 | 项目角色管理 | /project-role | ADMIN | 阶段1 |
| 7 | 项目列表 | /project/list | ADMIN/PM/PD | 阶段1 |
| 8 | 项目角色分配 | /project/:id/roles | ADMIN/PM/PD | 阶段1 |
| 9 | 岗位配置 | /position-config | ADMIN | 阶段1 |
| 10 | 项目KPI指标库 | /kpi-config/project | ADMIN/PM/PD | 阶段1 |
| 11 | 职能KPI指标库 | /kpi-config/functional | ADMIN | 阶段1 |
| 12 | 职能关系配置 | /leader-config | ADMIN | 阶段1 |
| 13 | 项目参与录入 | /participation | 全员(权限区分) | 阶段2 |
| 14 | 任务列表 | /tasks | 评估人/PM | 阶段2 |
| 15 | 考核打分(项目) | /assessment/score/project | 评估人 | 阶段2 |
| 16 | 考核打分(职能) | /assessment/score/functional | 直属上级 | 阶段2 |
| 17 | 考核校准(PD) | /assessment/calibrate | PD | 阶段2 |
| 18 | 总裁确认 | /assessment/confirm | 总裁 | 阶段2 |
| 19 | 考核结果 | /assessment/result | 全员(员工限本人+确认后) | 阶段2 |
| 20 | 仪表盘(全员) | /dashboard | 全员（阶段2：待处理任务+考核进度；阶段3：统计分析） | 阶段2+ |
| 21 | 历史考核记录 | /history | 员工/PM/PD/总裁 | 阶段3 |
| 22 | 申诉管理 | /appeal | 全员(权限区分) | 阶段3 |
| 23 | 报表导出 | /report/export | ADMIN/PM/PD/总裁 | 阶段3 |

> 页面交互细节参考PRD §7.2（项目参与录入页、考核打分页、KPI指标库页、汇总视图页、任务列表页、历史记录页）。此处不赘述以避免文档膨胀。

---

# CEO Review 决策记录

Reviewed by /plan-ceo-review on 2026-05-18 | Mode: HOLD SCOPE | 共计14项决定

## Section 1: Architecture — 3 issues resolved

| ID | 决策 | 理由 |
|----|------|------|
| D1 | 阶段1保存岗位配置时校验 `project_assessor_roles` 引用的角色代码在 `project_role` 表中存在 | 防止配置错误在阶段2考核生成时才发现 |
| D2 | `project` 表增加 `version` 字段，PM确认阶段和ADMIN重置阶段使用乐观锁 | 防止并发确认导致数据不一致 |
| D3 | 阶段1员工删除采用物理删除 | 阶段1无考核数据依赖，无需软删除 |

## Section 2: Error & Rescue — 2 issues resolved

| ID | 决策 | 理由 |
|----|------|------|
| D4 | 批量生成考核任务后输出差异报告：正常员工任务已生成，异常员工汇总到ADMIN仪表盘提醒，手动补配置后一键补生成 | 不阻断正常流程，异常不静默丢失 |
| D5 | 审批通过和考核任务生成在同一数据库事务中（`@Transactional`），一起提交或一起回滚 | 防止"审批通过但无考核任务"的数据不一致 |

## Section 3: Security — 2 issues resolved

| ID | 决策 | 理由 |
|----|------|------|
| D6 | API层使用 `@PreAuthorize` + SpEL 表达式做行级数据权限过滤，普通员工只能访问本人数据 | 防止IDOR攻击（修改URL中employeeId查看他人数据） |
| D7 | 阶段1引入 Spring Security 标准方案：CSRF token + bcrypt(12) 密码编码器。Phase 2对接SSO时只替换认证Provider | 安全基线从第一天建立，不积累安全债务 |

## Section 4: Data Flow & Edge Cases — 2 issues resolved

| ID | 决策 | 理由 |
|----|------|------|
| D8 | 所有配置表（`position_assessment_config`、`project_kpi_config`、`func_kpi_config`、`project_role_assignment`）增加 `version` 字段，统一乐观锁保护 | 与项目表的锁策略一致，防止静默覆盖 |
| D9 | 所有列表/表格页面统一空状态设计规范：插画/图标 + 引导文案 + 主要CTA按钮 | 低成本提升首次使用体验 |

## Section 5: Code Quality — 1 issue resolved

| ID | 决策 | 理由 |
|----|------|------|
| D10 | `project_assessor_roles` 阶段1就用关联表 `position_assessor_role_config(position_config_id, role_code)`，不采用逗号分隔字符串 | 避免第一范式违反，查询可用索引，无需阶段2数据迁移 |

## Section 6: Test — 1 issue resolved

| ID | 决策 | 理由 |
|----|------|------|
| D11 | 算分公式使用JUnit参数化测试，expected value来源于Excel公式计算结果，允许±0.001浮点容差 | HR验收最重要的信心来源——确保系统算分=Excel算分 |

## Section 7: Performance — No issues

阶段2应用层透视在200人规模下性能可控，阶段3数据量增长后引入物化视图。

## Section 8: Observability — 1 issue resolved

| ID | 决策 | 理由 |
|----|------|------|
| D12 | 阶段1建立统一日志规范：Controller AOP拦截记录请求日志（URL/用户/耗时/状态码），Service层关键操作记录业务日志，SLF4J + Logback JSON格式 | 成本极低（一个AOP切面），排查问题时避免靠猜 |

## Section 9: Deployment — 1 issue resolved

| ID | 决策 | 理由 |
|----|------|------|
| D13 | 项目初始化就配置 Flyway 做数据库迁移版本管理。所有DDL写成 `V1__...sql`, `V2__...sql` | 3阶段演进中保证各环境数据库一致，消除手动SQL执行风险 |

## Section 10: Long-Term Trajectory — No issues

可逆性评分 4/5，三阶段架构无路径锁定，技术选型主流。

## Section 11: Design & UX — 1 issue resolved

| ID | 决策 | 理由 |
|----|------|------|
| D14 | 阶段1仪表盘增加「开始配置」入口，启动7步配置向导（项目角色→导入员工→创建项目→分配角色→配置KPI→配置岗位→创建周期）。不改变现有页面，仅在仪表盘增加向导入口 | 验收标准"HR在2小时内独立完成首次配置"的关键支撑——无向导的话学习曲线太高 |

## 设计文档同步更新 ✅ 全部完成

| 更新项 | 影响位置 | 状态 |
|--------|----------|------|
| `project` 表增加 `version` 字段 | 附录B project表 | ✅ |
| 所有配置表增加 `version` 字段 | 附录B project_role_assignment / position_assessment_config / project_kpi_config / func_kpi_config | ✅ |
| `project_assessor_roles` 改为关联表 `position_assessor_role_config` | 附录B 新增表定义；正文 Data Model 关系图 → 新关联表 | ✅ |
| 阶段确认/配置编辑并发增加乐观锁冲突处理 | 正文 "周期中异常处理" 新增2行 | ✅ |
| 考核任务生成增加差异报告机制 + 事务原子性 | 正文 "考核关系自动生成算法" 重写 | ✅ |
| Spring Security / CSRF / bcrypt / @PreAuthorize | 正文 "Security & Access Control" 扩展 | ✅ |
| 统一日志规范（Controller AOP + JSON格式） | 正文 "Proposed Technical Stack" 新增日志行 | ✅ |
| Flyway 迁移管理 | 正文 "Proposed Technical Stack" 新增迁移行 | ✅ |
| Excel交叉验证测试策略 | 正文 "Testing Strategy" 新增交叉验证段 | ✅ |
| 配置向导 + 空状态设计规范 | 正文 "阶段1架构图" 新增2项；附录D 新增配置向导页 | ✅ |
| 仪表盘提前到阶段1 | 附录D 仪表盘改为阶段1（配置进度+差异报告）；阶段2+ 另有一行全员仪表盘 | ✅ |
| API并发控制（乐观锁+409） | 正文 "API Design Principles" 新增并发控制条 | ✅ |

- 你对"配置先行"的坚持是对的——在考核流程之前把地基打牢，说明你理解企业系统的核心复杂度不在流程而在规则
- 你选择逐条回应前提而不是全部同意，说明你在认真思考每个设计决策的tradeoff，不是照单全收PRD
- 你在"项目角色动态可配置"上很坚定（刚需），但在"通知双通道"上很务实（MVP先不做），说明你能区分真正的刚需和锦上添花
- PRD写得详尽但你没有被它框住——你主动意识到文档冗长对后续工作的困扰，这比闷头按文档开发强得多


## 变更记录

### 2026-05-27：配置向导改为仪表盘卡片引导

- 移除 /setup-wizard 路由及组件，仪表盘不再有"开始配置"按钮。
- 改为仪表盘显示7张配置卡片（员工管理、项目角色、项目管理、角色分配、KPI指标、岗位配置、考核周期），点击卡片跳转到对应独立页面。
- 配置进度基于后端 /dashboard/config-progress 接口，卡片显示"已配置"状态。
- 原任务 T20 取消，文档中以实际实现为准。
- 此变更不影响员工管理、KPI配置等核心功能。

### 2026-05-29：新增岗位分类管理模块

| 项目 | 内容 |
|------|------|
| 背景 | Phase 1 中原岗位分类仅为配置表的文本字段，无法独立管理，导致员工、职能KPI等下栏与岗位配置不一致。 |
| 变更类型 | 新增功能模块 |
| 新增文件 | 后端：`positioncategory/` 包下 5 个文件；前端：`PositionCategoryPage.jsx` + 路由；迁移：`V6__create_position_category_table.sql` |
| 修改文件 | 前端 4 个文件的下拉数据源改为动态接口；`AppLayout.jsx` 添加菜单项 |
| 影响范围 | 不破坏已有功能，原有 `position_assessment_config.category` 保留文本值，与新表并存。 |
| 验收标准 | HR 可独立增删改查分类，所有相关下拉框实时同步，删除被引用分类时给出错误提示。 |

此变更完善了岗位分类的独立维护能力，解决了 UAT 中提出的体验问题。