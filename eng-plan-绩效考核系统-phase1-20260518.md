# 工程规划 — 项目制绩效考核系统 · 阶段1 配置中心

Generated by /plan-eng-review on 2026-05-18
Branch: master | 范围: 阶段1 配置中心 | 审查对象: design-绩效考核系统-20260518.md

---

## 〇、我该怎么用这个规划

如果你没有代码基础，这份规划的使用方式如下：

### 第一步：理解结构

先看 **第一章"模块地图"**——它像一张地图，告诉你系统由哪些"积木"组成。后端积木（Java）负责存数据、算逻辑；前端积木（React）负责显示页面、接收操作。每个积木都有明确的包路径或URL，找人问问题时直接说"employee模块的保存接口报错了"即可。

### 第二步：理解接口

看 **第二章"API接口说明"**——它列出了每个操作对应的后端接口。场景化描述用大白话解释"什么时候会调用这个接口"。比如："HR在员工列表页点击新增按钮，填写表单后点确定，前端就调用 POST /api/v1/employees"。

### 第三步：按顺序开发

看 **第五章"实施任务清单"**——任务按优先级 P1→P2→P3 排列。P1任务是地基，必须最先做；P2任务是核心功能，紧随其后；P3任务是锦上添花。每完成一个任务，在 `[ ]` 里打勾。

### 第四步：后续开发

当你需要改某个功能时，告诉我"改 employee 模块的某某功能"，我能直接定位到 `employee/` 目录。因为每个模块是独立目录，不会影响其他模块。所有代码都有白话注释，你可以直接读注释理解每段代码做什么。

### 第五步：测试验证

看 **第六章"测试计划"**——它告诉你每个功能要测什么场景。比如"员工新增"要测：正常新增、工号重复、必填字段缺失三种情况。

---

## 一、模块地图（物理拆分）

### 1.1 后端模块

基础包路径：`com.jifeng.assessment`

```
com.jifeng.assessment
│
├── employee/          ← 员工管理：增删改查、状态、直属上级
│   ├── EmployeeController.java
│   ├── EmployeeService.java        (extends BaseService)
│   ├── EmployeeMapper.java         (extends BaseMapper<Employee>)
│   ├── Employee.java               (实体类 → employee表)
│   ├── EmployeeDTO.java            (返回给前端的对象，不含敏感字段)
│   └── EmployeeValidator.java      (工号唯一性等自定义校验)
│
├── user/              ← 用户与角色：创建系统用户、分配角色
│   ├── UserController.java
│   ├── UserService.java
│   ├── UserMapper.java
│   ├── User.java                  (实体类 → user表)
│   └── UserRoleMapper.java        (用户角色关联 → user_role表)
│
├── projectrole/       ← 项目角色：动态增删改查、启用停用、被引用时禁止删除
│   ├── ProjectRoleController.java
│   ├── ProjectRoleService.java
│   ├── ProjectRoleMapper.java
│   └── ProjectRole.java           (实体类 → project_role表)
│
├── project/           ← 项目管理：创建项目、PM确认阶段(乐观锁)、ADMIN重置
│   ├── ProjectController.java
│   ├── ProjectService.java
│   ├── ProjectMapper.java
│   └── Project.java               (实体类 → project表)
│
├── roleassignment/    ← 项目角色分配：给项目分配角色人员、标记PD负责人
│   ├── RoleAssignmentController.java
│   ├── RoleAssignmentService.java
│   ├── RoleAssignmentMapper.java
│   └── ProjectRoleAssignment.java (实体类 → project_role_assignment表)
│
├── position/          ← 岗位考核配置：考核权重、考核人角色关联、是否纳入项目制
│   ├── PositionConfigController.java
│   ├── PositionConfigService.java
│   ├── PositionConfigMapper.java
│   ├── PositionAssessorRoleMapper.java  (考核人角色关联表)
│   ├── PositionAssessmentConfig.java    (实体类)
│   └── PositionAssessorRoleConfig.java  (实体类，关联表)
│
├── kpi/               ← KPI指标配置：项目KPI和职能KPI指标库、权重校验
│   ├── KpiConfigController.java
│   ├── KpiConfigService.java
│   ├── ProjectKpiMapper.java
│   ├── FuncKpiMapper.java
│   ├── WeightValidator.java       (权重校验工具：0.3+0.3+0.4必须=1.0)
│   ├── ScoreCalculator.java       (算分公式引擎：纯函数，提前到Phase1以满足D11)
│   ├── ProjectKpiConfig.java      (实体类 → project_kpi_config表)
│   └── FuncKpiConfig.java         (实体类 → func_kpi_config表)
│
├── system/            ← 系统参数：key-value配置（凭证必填开关、退回次数上限等）
│   ├── SystemParamController.java
│   ├── SystemParamService.java
│   ├── SystemParamMapper.java
│   └── SystemParam.java           (实体类 → system_param表)
│
├── period/            ← 考核周期：创建编辑关闭周期、同期唯一性校验
│   ├── PeriodController.java
│   ├── PeriodService.java
│   ├── PeriodMapper.java
│   └── AssessmentPeriod.java      (实体类 → assessment_period表)
│
├── wizard/            ← 配置向导：7步分步保存、进度查询、断点续配
│   ├── WizardController.java
│   ├── WizardService.java
│   ├── WizardProgressMapper.java   (向导进度表 → wizard_progress表)
│   ├── WizardProgress.java         (实体类：user_id/current_step/completed_steps)
│   └── WizardProgressDTO.java      (向导进度返回对象)
│
├── import/            ← 批量导入：Excel/CSV员工导入、校验去重、试运行预览导入
│   ├── ImportController.java
│   ├── ImportService.java
│   └── ImportResultDTO.java       (导入结果：成功数/失败数/错误明细)
│
├── dashboard/         ← 仪表盘：配置进度统计、差异报告提醒
│   ├── DashboardController.java
│   └── DashboardService.java
│
├── security/          ← 安全配置：Spring Security、CSRF防护、登录认证
│   ├── SecurityConfig.java        (安全规则配置)
│   ├── CsrfConfig.java            (CSRF token配置)
│   ├── AuthController.java        (登录/登出接口)
│   └── UserDetailsServiceImpl.java(用户认证信息加载)
│
├── common/            ← 公共组件：所有模块共用的东西
│   ├── BaseService.java           (通用增删改查+分页+乐观锁基类)
│   ├── BaseController.java        (通用接口包装基类)
│   ├── PageQuery.java             (分页请求参数：page/size/sortBy)
│   ├── PageResult.java            (分页返回结果：total/list/page/size)
│   ├── ApiResponse.java           (统一返回体：{code, message, data})
│   ├── GlobalExceptionHandler.java(全局异常处理 → 转为标准JSON错误)
│   ├── BusinessException.java     (业务异常类)
│   ├── ControllerLogAspect.java   (AOP日志切面：记录每个API的URL/用户/耗时)
│   └── ValidationUtils.java       (通用校验工具：权重和=1、浮点容差等)
│
└── infra/             ← 基础设施：数据库迁移、缓存、框架配置
    ├── FlywayConfig.java          (Flyway迁移配置)
    ├── RedisConfig.java           (Redis连接配置)
    ├── MybatisPlusConfig.java     (MyBatis-Plus分页插件、乐观锁插件)
    └── WebMvcConfig.java          (CORS、拦截器等Web配置)
```

### 1.2 前端模块

```
src/
├── pages/
│   ├── Login/                     ← URL: /login
│   │   └── LoginPage.jsx          (用户名+密码登录表单)
│   │
│   ├── Dashboard/                 ← URL: /dashboard
│   │   └── DashboardPage.jsx      (配置进度卡片+差异报告红点+向导入口)
│   │
│   ├── SetupWizard/               ← URL: /setup-wizard
│   │   ├── SetupWizardPage.jsx    (7步向导容器：步骤条+当前步骤内容)
│   │   ├── StepProjectRole.jsx    (步骤1：配置项目角色)
│   │   ├── StepImportEmployee.jsx (步骤2：导入员工)
│   │   ├── StepCreateProject.jsx  (步骤3：创建项目)
│   │   ├── StepAssignRole.jsx     (步骤4：分配项目角色)
│   │   ├── StepKpiConfig.jsx      (步骤5：配置KPI指标)
│   │   ├── StepPositionConfig.jsx (步骤6：配置岗位考核)
│   │   └── StepCreatePeriod.jsx   (步骤7：创建考核周期)
│   │
│   ├── Employee/                  ← URL: /employee/list
│   │   └── EmployeeListPage.jsx   (员工表格+搜索+新增编辑删除)
│   │
│   ├── UserRole/                  ← URL: /user-role
│   │   └── UserRolePage.jsx       (用户列表+角色分配)
│   │
│   ├── ProjectRole/               ← URL: /project-role
│   │   └── ProjectRolePage.jsx    (项目角色卡片列表+增删改查)
│   │
│   ├── Project/                   ← URL: /project/list
│   │   └── ProjectListPage.jsx    (项目表格+阶段确认/重置按钮)
│   │
│   ├── RoleAssignment/            ← URL: /project/:id/roles
│   │   └── RoleAssignmentPage.jsx (角色人员分配面板+PD标记)
│   │
│   ├── PositionConfig/            ← URL: /position-config
│   │   └── PositionConfigPage.jsx (岗位配置表格+权重滑块+角色多选)
│   │
│   ├── KpiConfig/                 ← URL: /kpi-config/project + /kpi-config/functional
│   │   ├── ProjectKpiPage.jsx     (项目KPI：角色×阶段×指标)
│   │   └── FuncKpiPage.jsx        (职能KPI：岗位×指标)
│   │
│   └── LeaderConfig/              ← URL: /leader-config
│       └── LeaderConfigPage.jsx   (直属上级关系表格+批量编辑)
│
└── components/                    ← 公共组件（被多个页面引用）
    ├── EmptyState.jsx             (统一空状态：图标+引导文案+CTA按钮)[D9]
    ├── AppLayout.jsx              (整体布局：侧边栏+顶栏+内容区)
    ├── PageHeader.jsx             (页面标题栏：标题+面包屑+操作按钮)
    └── ConfirmModal.jsx           (统一确认弹窗：409冲突/删除确认等)
```

---

## 二、API接口说明（带场景化描述）

### 2.1 员工管理 — employee

**场景：HR需要把继峰现有的200-500名员工数据录入系统，后续可能新增离职员工或修改岗位信息。**

| 方法 | 路径 | 场景化描述 |
|------|------|-----------|
| GET | `/api/v1/employees?page=1&size=20&keyword=&category=&status=` | HR打开员工列表页，页面自动加载第一页20条数据。HR可以在搜索框输入姓名或工号进行模糊搜索，也可以按岗位分类或状态过滤 |
| GET | `/api/v1/employees/{employeeId}` | HR点击某个员工的"编辑"按钮，前端根据工号获取该员工的完整信息，回填到编辑表单里 |
| POST | `/api/v1/employees` | HR点击"新增员工"按钮，填写工号/姓名/邮箱/岗位/部门/直属上级后点确定。后端校验工号不重复、必填字段完整、直属上级工号存在 |
| PUT | `/api/v1/employees/{employeeId}` | HR修改某个员工的信息（如岗位变更、状态改为离职）后点保存。version字段用于防止两个HR同时编辑同一个人 |
| DELETE | `/api/v1/employees/{employeeId}` | HR删除某个员工。但阶段1只有物理删除——如果该员工已被分配到某个项目的角色，则返回错误提示"该员工已分配项目角色，请先取消分配" |

> 批量导入员工使用独立的 import 模块（见 2.11），不在 employee 模块中重复提供简易导入。

### 2.2 用户与角色 — user

**场景：HR为使用系统的人创建登录账号，并分配角色。一个账号可以有多个角色（如一个人同时是PM和评估人）。**

| 方法 | 路径 | 场景化描述 |
|------|------|-----------|
| GET | `/api/v1/users?page=1&size=20` | HR打开用户管理页，看到所有系统用户列表 |
| POST | `/api/v1/users` | HR创建新用户：选择关联的员工、设置用户名和初始密码。密码用bcrypt(12)加密存储 |
| PUT | `/api/v1/users/{userId}/roles` | HR勾选/取消用户的角色（ADMIN/总裁/PD/PM/评估人/员工），保存后立即生效 |
| POST | `/api/v1/auth/login` | 任何用户打开系统首先看到登录页，输入用户名+密码。登录成功获得Session，后续请求自动携带 |
| POST | `/api/v1/auth/logout` | 用户点击退出，Session失效 |

### 2.3 项目角色管理 — projectrole

**场景：继峰的组织架构中项目角色是动态变化的——今天可能是"PDL/PQL/Launch"，明天可能新增"质量代表"。HR需要能自己增删改角色，不需要开发人员介入。**

| 方法 | 路径 | 场景化描述 |
|------|------|-----------|
| GET | `/api/v1/project-roles?isActive=true` | HR打开项目角色管理页，看到所有已启用的角色列表（如PDL、PQL、Launch等） |
| POST | `/api/v1/project-roles` | HR点击"新增角色"，填写角色代码（如"QA"）和角色名称（如"质量代表"），保存后立即出现在列表里 |
| PUT | `/api/v1/project-roles/{roleCode}` | HR修改某个角色的名称或描述。version字段防并发编辑 |
| DELETE | `/api/v1/project-roles/{roleCode}` | HR想删除一个不再使用的角色。但如果该角色已被岗位配置引用（在position_assessor_role_config表里），后端拒绝删除并提示"角色XXX被X个岗位配置引用，无法删除" |
| PUT | `/api/v1/project-roles/{roleCode}/toggle` | HR点击"停用"开关，该角色不再出现在下拉选项中，但历史引用不受影响 |

### 2.4 项目管理 — project

**场景：PM在每个考核周期开始前需要确认自己负责的项目的阶段（P2/P3/P4/P5），确认后阶段被锁定。如果PM确认错了，ADMIN可以强制重置。**

| 方法 | 路径 | 场景化描述 |
|------|------|-----------|
| GET | `/api/v1/projects?page=1&size=20&stage=&status=` | PM或HR打开项目列表，可以按项目阶段和状态筛选 |
| POST | `/api/v1/projects` | HR创建新项目，填写项目编码/名称/阶段/说明。项目编码全局唯一 |
| PUT | `/api/v1/projects/{projectCode}/confirm-stage` | PM审核项目信息无误后，点击"确认阶段"按钮。后端用乐观锁检查version——如果确认时version已变（别人同时操作了），返回409"数据已被他人修改，请刷新后重试" |
| PUT | `/api/v1/projects/{projectCode}/reset-stage` | ADMIN发现PM确认的阶段有误，点击"强制重置"按钮。项目阶段回到未确认状态，PM需要重新确认。只有ADMIN角色能执行此操作 |

### 2.5 项目角色分配 — roleassignment

**场景：HR为每个项目分配角色人员——比如项目PRJ2025001的PDL角色是张三、PQL角色是李四。同时标记哪个PDL是"PD负责人"。**

| 方法 | 路径 | 场景化描述 |
|------|------|-----------|
| GET | `/api/v1/projects/{projectCode}/assignments` | HR进入某个项目的角色分配页，看到该项目下所有角色及其分配的人员列表 |
| POST | `/api/v1/projects/{projectCode}/assignments` | HR在"PDL"角色下添加张三。如果张三已经被分配给该项目的PDL角色，后端提示"该员工已分配此角色" |
| PUT | `/api/v1/projects/{projectCode}/assignments/{id}/toggle-primary-pd` | HR勾选某个PDL为"PD负责人"。一个项目只能有一个负责人，如果原来已有负责人则自动取消 |
| DELETE | `/api/v1/projects/{projectCode}/assignments/{id}` | HR移除某个角色分配。如果该员工的该角色已关联生成考核任务（阶段2的事），阶段1直接删除即可 |

### 2.6 岗位考核配置 — position

**场景：HR为每个岗位（如"整椅研发岗"）配置考核规则：项目考核占多少权重、职能考核占多少权重、由哪些项目角色来考核、默认的项目角色是什么。**

| 方法 | 路径 | 场景化描述 |
|------|------|-----------|
| GET | `/api/v1/position-configs?page=1&size=20&category=&position=` | HR打开岗位配置页，按岗位分类和名称筛选 |
| POST | `/api/v1/position-configs` | HR新增岗位配置：选择岗位分类+岗位名称，设置项目权重70%/职能权重30%，勾选考核人角色（如PDL+PQL），选择是否纳入项目制。保存时后端校验：(1)权重之和=100% (2)考核人角色代码在project_role表中存在(D1) |
| PUT | `/api/v1/position-configs/{id}` | HR修改已有配置，version字段防并发覆盖 |
| GET | `/api/v1/position-configs/{id}/assessor-roles` | 查看某个岗位配置关联了哪些考核人角色 |
| POST | `/api/v1/position-configs/{id}/assessor-roles` | 给岗位配置新增一个考核人角色（如新增"Launch"） |
| DELETE | `/api/v1/position-configs/{id}/assessor-roles/{assessorRoleId}` | 移除某个考核人角色关联 |

### 2.7 KPI指标配置 — kpi

**场景：HR为"项目考核"和"职能考核"分别配置KPI指标库。比如项目KPI：PDL角色在P2阶段需要考核"技术方案质量"和"进度计划合理性"，指标权重之和必须为100%。**

| 方法 | 路径 | 场景化描述 |
|------|------|-----------|
| GET | `/api/v1/kpi-configs/project?roleCode=&stage=&isActive=true` | HR打开项目KPI配置页，按角色+阶段筛选，看到该组合下的所有KPI指标及权重 |
| POST | `/api/v1/kpi-configs/project` | HR为"PDL+P2"新增一个指标"技术方案质量"，权重30%，设置1-5分的评价标准。保存时后端校验：同一角色+同一阶段下所有权重之和≤100% |
| PUT | `/api/v1/kpi-configs/project/{id}` | HR修改指标权重或评价标准。修改后所有权重之和仍需=100% |
| PUT | `/api/v1/kpi-configs/project/{id}/toggle` | HR停用某个不再使用的KPI指标 |
| GET | `/api/v1/kpi-configs/functional?category=&position=` | HR打开职能KPI配置页，按岗位分类+岗位名称筛选 |
| POST | `/api/v1/kpi-configs/functional` | HR为"研发技术类+整椅研发岗"新增职能KPI指标，逻辑同项目KPI |

### 2.8 系统参数 — system

**场景：系统有一些全局开关和参数，HR可以在一个统一的页面上修改，不需要找人改代码。比如"评分是否必须上传凭证"、"PD退回次数上限"。**

| 方法 | 路径 | 场景化描述 |
|------|------|-----------|
| GET | `/api/v1/system-params` | HR打开系统参数页，看到所有key-value的列表 |
| PUT | `/api/v1/system-params` | HR修改参数值（如把MAX_RETURN_TIMES从3改成5），一次可以改多个。保存后立即生效 |

### 2.9 考核周期 — period

**场景：HR每次考核前需要创建一个"考核周期"（如"2026年上半年考核"），设置起止日期。同一时间只能有一个活跃周期。**

| 方法 | 路径 | 场景化描述 |
|------|------|-----------|
| GET | `/api/v1/periods?status=` | HR查看所有考核周期，可以按状态筛选（进行中/已完成） |
| POST | `/api/v1/periods` | HR创建新周期：填写周期名称、开始日期、结束日期。如果当前已有未关闭的周期，后端拒绝并提示"请先关闭当前活跃周期" |
| PUT | `/api/v1/periods/{periodId}/close` | HR在考核全部结束后点击"关闭周期"，周期状态变为COMPLETED，不可再修改 |

### 2.10 配置向导 — wizard

**场景：HR第一次使用系统时，从仪表盘点击"开始配置"，进入7步向导。每一步填完点"下一步"自动保存，中途可以离开，回来后自动恢复到上次的步骤。**

| 方法 | 路径 | 场景化描述 |
|------|------|-----------|
| GET | `/api/v1/wizard/progress` | 查询HR当前的配置进度。如果从未使用过向导，返回currentStep=0；如果已完成了3步，返回currentStep=4（下一步是第4步） |
| POST | `/api/v1/wizard/step/project-role` | 步骤1：HR配置项目角色（如创建PDL/PQL/Launch）。保存后返回下一步是步骤2 |
| POST | `/api/v1/wizard/step/import-employee` | 步骤2：HR上传员工Excel文件导入。保存后返回导入结果报告 |
| POST | `/api/v1/wizard/step/project` | 步骤3：HR创建第一个项目（填写编码/名称/阶段） |
| POST | `/api/v1/wizard/step/role-assignment` | 步骤4：HR为项目分配角色人员 |
| POST | `/api/v1/wizard/step/kpi` | 步骤5：HR配置KPI指标（项目KPI+职能KPI） |
| POST | `/api/v1/wizard/step/position` | 步骤6：HR配置岗位考核规则 |
| POST | `/api/v1/wizard/step/period` | 步骤7：HR创建第一个考核周期。这是最后一步，完成后向导标记为已完成 |
| PUT | `/api/v1/wizard/reset` | HR想重新配置，点击"重置向导"，所有向导步骤回到初始状态（不会删除已创建的数据） |

### 2.11 批量导入 — import

**场景：HR从继峰现有系统导出员工Excel，通过导入功能批量录入，避免逐条手工输入。**

| 方法 | 路径 | 场景化描述 |
|------|------|-----------|
| POST | `/api/v1/import/employees/preview` | HR上传Excel后，系统先预览前10条数据，让HR确认列映射是否正确（Excel的"A列"对应"工号"还是"姓名"？） |
| POST | `/api/v1/import/employees/execute` | HR确认列映射无误后点"开始导入"。系统逐行校验（工号必填/邮箱格式/直属上级存在），返回报告：成功X条/失败X条，失败的具体原因 |

### 2.12 仪表盘 — dashboard

**场景：ADMIN登录后首先看到仪表盘，上面显示：(1)配置完成度——还差几步才算配置完整 (2)差异报告红点——阶段2批量生成考核任务后哪些员工有问题。**

| 方法 | 路径 | 场景化描述 |
|------|------|-----------|
| GET | `/api/v1/dashboard/config-progress` | 仪表盘加载配置进度：员工数/项目角色数/项目数/岗位配置数/KPI指标数。如果某项为0，显示"待配置"状态并链接到对应的配置页 |
| GET | `/api/v1/dashboard/diff-report` | 阶段2批量生成考核任务后，这里显示异常清单（缺岗位配置的员工、无考核人的员工等）。阶段1此接口返回空列表 |

---

## 三、代码注释规则

后续所有生成的代码严格执行以下注释规则：

### 3.1 后端Java文件

**每个Java文件开头3行：**
```java
// 模块用途：<这个文件负责什么业务功能，一句话>
// 依赖文件：<依赖哪些其他模块的哪个类，没有写"无">
// 修改注意：<修改这个文件时需要注意什么，比如只改Service方法内部逻辑>
```

**每个public方法上方一行白话注释：**
```java
// 功能：<这个方法做什么，输入什么，返回什么，白话描述>
public EmployeeDTO createEmployee(CreateEmployeeRequest request) {
```

**关键逻辑内行注释：**
```java
// 权重校验：0.3+0.3+0.4在计算机里可能等于0.999999，所以用BigDecimal且允许±0.001容差
if (sum.subtract(BigDecimal.ONE).abs().compareTo(TOLERANCE) > 0) {
```

### 3.2 前端React文件

**每个组件文件开头：**
```jsx
{/* 模块用途：员工列表页——展示所有员工的分页表格，支持搜索筛选和CRUD操作 */}
{/* 依赖组件：EmptyState, ConfirmModal, PageHeader */}
{/* 修改注意：表格列定义在COLUMNS常量里，修改列只需改此处 */}
```

**每个数据展示区域使用 `<div id="xxx">` 包裹：**
```jsx
<div id="employee-search-area">
  {/* 功能：搜索栏——输入姓名或工号后按回车触发搜索 */}
  <Input.Search placeholder="搜索姓名或工号" onSearch={handleSearch} />
</div>

<div id="employee-table-area">
  {/* 功能：员工数据表格——分页展示，点击行操作按钮触发编辑/删除 */}
  <Table columns={COLUMNS} dataSource={employees} />
</div>
```

**每个事件处理函数上方一行白话注释：**
```jsx
// 功能：点击新增按钮弹出表单弹窗，用户填写后调用POST /api/v1/employees创建员工
const handleAddEmployee = async (values) => {
```

---

## 四、NOT in Scope（明确不做）

| 项目 | 理由 |
|------|------|
| 阶段2考核流程（申报/打分/校准/确认） | 依赖阶段1数据模型，后续会话规划 |
| 阶段3增强功能（申诉/报表/通知/归档） | 依赖阶段2流程闭环 |
| 邮件通知 | 设计文档明确阶段3实现 |
| 完整可配置审批流 | 阶段2用开关，阶段3升级 |
| SSO/LDAP集成 | 阶段1用用户名+密码，阶段2+评估对接 |
| 数据归档 | 阶段3引入 |
| 审计日志表 | 阶段3引入，但Controller AOP日志阶段1就做 |

## 五、What Already Exists

Greenfield项目，无已有代码。设计文档 `design-绩效考核系统-20260518.md` 已完成CEO Review（14项决定全部关闭），作为本次规划的单一事实来源。

---

## 六、实施任务清单

### 6.1 基础设施（必须先做）

- [x] **T1 (P1, ~30min / ~5min)** — 项目初始化：Spring Boot 3 脚手架 + 依赖配置 ✅ 2026-05-19
  - 文件: `pom.xml`, `application.yml`, `AssessmentApplication.java`
  - 验证: `mvn spring-boot:run` 启动成功

- [x] **T2 (P1, ~30min / ~5min)** — Flyway迁移管理初始化 + DDL + 种子数据 ✅ 2026-05-19
  - 文件: `db/migration/V1__init.sql` (13张表), `db/migration/V2__add_version_columns.sql` (7张表乐观锁字段), `db/migration/V3__seed_data.sql` (默认system_param + ADMIN员工记录)
  - 验证: 启动应用 → Flyway自动执行迁移 → 数据库表全部创建 → system_param有默认值 → admin用户由DataInitializer创建

- [x] **T3 (P1, ~1h / ~10min)** — Spring Security + CSRF + bcrypt(12) 配置 ✅ 2026-05-19
  - 文件: `security/SecurityConfig.java`, `security/AuthController.java`, `security/UserDetailsServiceImpl.java`, `security/DataInitializer.java`
  - 验证: 未登录访问 `/api/*` → 401；POST 无 CSRF token → 403

- [x] **T4 (P1, ~30min / ~5min)** — AOP日志切面 + Logback JSON格式 ✅ 2026-05-19
  - 文件: `common/ControllerLogAspect.java`, `logback-spring.xml`
  - 验证: 任意API调用 → 控制台输出 JSON 格式请求日志

- [x] **T5 (P1, ~30min / ~5min)** — 公共基类 + 统一返回体 + 全局异常处理 ✅ 2026-05-19
  - 文件: `common/BaseService.java`, `common/BaseController.java`, `common/ApiResponse.java`, `common/GlobalExceptionHandler.java`, `common/BusinessException.java`, `common/PageQuery.java`, `common/PageResult.java`, `common/ValidationUtils.java`, `infra/MybatisPlusConfig.java`
  - 验证: 故意触发异常 → 返回标准JSON错误格式

### 6.2 核心功能模块

- [x] **T6 (P1, ~1h / ~10min)** — employee 模块：员工CRUD + 校验
  - 文件: `employee/` 全部6个文件
  - 验证: POST创建 → GET列表 → PUT更新 → DELETE删除；工号重复返回409

- [x] **T7 (P1, ~30min / ~5min)** — user 模块：用户创建 + 角色分配
  - 文件: `user/` 全部5个文件
  - 验证: 创建用户 → 分配角色 → 登录成功

- [x] **T8 (P1, ~30min / ~5min)** — projectrole 模块：项目角色CRUD + 引用检查
  - 文件: `projectrole/` 全部4个文件
  - 验证: 创建角色 → 被岗位配置引用后 → 删除被拒绝

- [x] **T9 (P1, ~1h / ~10min)** — project 模块：项目管理 + 阶段确认乐观锁
  - 文件: `project/` 全部4个文件
  - 验证: PM确认阶段 → 并发确认返回409 → ADMIN强制重置

- [x] **T10 (P1, ~30min / ~5min)** — roleassignment 模块：角色分配 + PD标记
  - 文件: `roleassignment/` 全部4个文件
  - 验证: 分配人员 → 标记PD负责人 → 移除分配

- [x] **T11 (P1, ~1h / ~10min)** — position 模块：岗位配置 + 考核人角色关联 + 角色引用校验(D1)
  - 文件: `position/` 全部6个文件
  - 验证: 创建配置 → 引用不存在的角色返回错误 → 权重校验

- [x] **T12 (P1, ~1h / ~10min)** — kpi 模块：KPI指标库 + 权重校验 + 浮点精度
  - 文件: `kpi/` 全部7个文件
  - 验证: 新增KPI → 权重之和≠100%返回错误 → 0.3+0.3+0.4通过

- [x] **T13 (P1, ~30min / ~5min)** — system 模块：系统参数CRUD
  - 文件: `system/` 全部4个文件
  - 验证: 修改参数 → 读取参数 → 值已生效

- [x] **T14 (P1, ~30min / ~5min)** — period 模块：考核周期管理 + 活跃周期唯一约束
  - 文件: `period/` 全部4个文件
  - 验证: 创建周期 → 创建第二个活跃周期被拒绝 → 关闭第一个后再创建

- [x] **T15 (P1, ~1h / ~10min)** — wizard 模块：7步向导分步保存 + 进度查询 ✅ 2026-05-20
  - 文件: `wizard/` 全部3个文件
  - 验证: 逐步提交 → 中途离开 → 重新进入恢复到上次步骤 → 7步完成

- [ ] **T16 (P1, ~1h / ~10min)** — import 模块：Excel导入 + 校验 + 去重 + 试运行
  - 文件: `import/` 全部3个文件
  - 验证: 上传有效Excel → 预览 → 确认导入 → 查看导入报告

- [ ] **T17 (P1, ~30min / ~5min)** — dashboard 模块：配置进度统计
  - 文件: `dashboard/` 全部2个文件
  - 验证: 创建部分数据 → 仪表盘显示正确的完成度

### 6.3 前端页面

- [ ] **T18 (P1, ~2h / ~15min)** — 公共组件：AppLayout + EmptyState + PageHeader + ConfirmModal
  - 文件: `components/` 全部4个文件
  - 验证: EmptyState在新系统首次访问时正确显示

- [ ] **T19 (P1, ~1h / ~10min)** — Login + Dashboard 页面
  - 文件: `pages/Login/`, `pages/Dashboard/`
  - 验证: 登录 → 仪表盘显示配置进度 → 点击"开始配置"进入向导

- [ ] **T20 (P1, ~3h / ~20min)** — SetupWizard 页面(7步向导)
  - 文件: `pages/SetupWizard/` 全部8个文件
  - 验证: 首次登录 → 仪表盘点击"开始配置" → 7步走通

- [ ] **T21 (P1, ~30min / ~5min)** — EmployeeList 页面
  - 文件: `pages/Employee/`
  - 验证: 表格分页 → 搜索 → 新增 → 编辑 → 删除

- [ ] **T22 (P2, ~30min / ~5min)** — UserRole 页面
  - 文件: `pages/UserRole/`
  - 验证: 用户列表 → 勾选角色 → 保存

- [ ] **T23 (P2, ~30min / ~5min)** — ProjectRole 页面
  - 文件: `pages/ProjectRole/`
  - 验证: 角色卡片 → 新增 → 停用

- [ ] **T24 (P2, ~30min / ~5min)** — ProjectList 页面
  - 文件: `pages/Project/`
  - 验证: 项目表格 → PM确认阶段 → ADMIN重置

- [ ] **T25 (P2, ~30min / ~5min)** — RoleAssignment 页面
  - 文件: `pages/RoleAssignment/`
  - 验证: 选择角色 → 添加人员 → 标记PD负责人

- [ ] **T26 (P1, ~30min / ~5min)** — PositionConfig 页面
  - 文件: `pages/PositionConfig/`
  - 验证: 权重滑块 → 考核人角色多选 → 保存校验

- [ ] **T27 (P2, ~1h / ~10min)** — ProjectKpiConfig + FuncKpiConfig 页面
  - 文件: `pages/KpiConfig/`
  - 验证: 指标表格 → 权重校验实时提示 → 排序拖拽

- [ ] **T28 (P2, ~30min / ~5min)** — LeaderConfig 页面
  - 文件: `pages/LeaderConfig/`
  - 验证: 直属上级关系表格 → 批量编辑

### 6.4 测试

- [ ] **T29 (P1, ~2h / ~15min)** — 后端Service单元测试：所有模块核心方法
  - 文件: `src/test/java/com/jifeng/assessment/*/`
  - 验证: `mvn test` → 30+用例通过

- [ ] **T30 (P1, ~1h / ~10min)** — 后端集成测试：乐观锁行为 + 唯一约束
  - 文件: `src/test/java/com/jifeng/assessment/integration/`
  - 验证: 并发更新 → 409冲突；重复工号 → 约束异常

- [ ] **T31 (P1, ~1h / ~10min)** — 算分公式Excel交叉验证(D11)：10个参数化用例
  - 文件: `src/test/java/com/jifeng/assessment/kpi/ScoreCalculatorTest.java`
  - 验证: 10个用例全部通过 ±0.001容差

- [ ] **T32 (P2, ~1h / ~15min)** — E2E测试：登录流程 + 7步配置向导完整走通
  - 文件: `e2e/login.spec.js`, `e2e/setup-wizard.spec.js`
  - 验证: `npx playwright test` → 2个用例通过

---

## 七、测试计划

### 7.1 受影响页面/路由

| URL | 测试重点 | 原因 |
|-----|---------|------|
| `/login` | 正确密码登录、错误密码提示、空字段提交 | 安全入口，失败提示必须清晰 |
| `/dashboard` | 首次访问显示引导空状态、配置50%进度、完成后状态 | 用户第一印象 |
| `/setup-wizard` | 7步走通、中途断点续配、跳过可选步骤 | HR首次配置的核心体验 |
| `/employee/list` | 新增(含校验)、编辑(含乐观锁冲突)、删除(含引用检查)、批量导入 | 配置中心最高频操作 |
| `/position-config` | 权重非100%拒绝、角色引用不存在拒绝 | 数据质量防线 |
| `/kpi-config/project` | 权重浮点精度验证、重复指标名拒绝 | 算分正确性的基础 |

### 7.2 关键交互验证

- 乐观锁冲突全流程：用户A打开编辑 → 用户B修改并保存 → 用户A保存时看到409 + 刷新提示
- 7步向导完整走通后仪表盘从"待配置"变为"配置完成"
- Excel导入空文件/超大文件/全重复ID的边界行为
- CSRF token缺失时POST请求返回403

### 7.3 关键路径

**路径1**：登录 → 仪表盘(首次引导) → 7步向导完整走通 → 仪表盘(配置完成)
**路径2**：员工管理全流程：新增 → 搜索 → 编辑 → 删除(引用检查) → 批量导入

---

## 八、错误与救援注册表

| 代码路径 | 故障模式 | 救援 | 用户看到 |
|----------|---------|------|---------|
| EmployeeService.create | 工号重复 | 返回409 | "工号EMP001已存在" |
| EmployeeService.update | version冲突 | 返回409 | "数据已被他人修改，请刷新后重试" |
| EmployeeService.delete | 员工已分配项目角色 | 返回400 | "该员工已分配项目角色，请先取消分配" |
| ProjectRoleService.delete | 角色被岗位配置引用 | 返回400 | "角色PDL被3个岗位配置引用，无法删除" |
| PositionConfigService.save | 考核人角色代码不存在 | 返回400 | "角色XXX在项目角色表中不存在"(D1) |
| KpiConfigService.save | 权重之和≠100% | 返回400 | "指标权重之和必须为100%（当前: 90%）" |
| KpiConfigService.save | 浮点精度0.3+0.3+0.4≠1.0 | BigDecimal容差通过 | 正常保存（内部修正误差） |
| ProjectService.confirmStage | version冲突 | 返回409 | "数据已被他人修改，请刷新后重试"(D2) |
| WizardService.saveStep | 步骤顺序跳过 | 返回400 | "请先完成第3步" |
| ImportService.execute | Excel为空/格式错误/超1000行 | 返回400 | "文件为空"/"不支持的文件格式"/"单次导入不能超过1000行" |
| AuthController.login | 密码错误 | 返回401 | "用户名或密码错误" |
| SecurityConfig | 未登录访问API | 返回401 | 重定向到/login |
| SecurityConfig | CSRF token缺失 | 返回403 | "CSRF token验证失败" |
| @PreAuthorize | 员工A访问员工B数据 | 返回403 | "无权访问"(D6) |

---

## 九、工作树并行策略

阶段1的15个后端模块中，employee/user/projectrole 三个模块完全独立，可以并行开发。

| 泳道 | 任务 | 依赖 |
|------|------|------|
| **Lane A**: 基础设施 | T1 → T2 → T3 → T4 → T5 | — |
| **Lane B**: 核心数据模块 | T6(employee) + T7(user) + T8(projectrole) → T9(project) → T10(roleassignment) | Lane A完成 |
| **Lane C**: 配置模块 | T11(position) + T12(kpi) + T13(system) 可并行 → T14(period) → T15(wizard) | Lane A完成 |
| **Lane D**: 辅助模块 | T16(import) + T17(dashboard) 可并行 | Lane B部分完成 |
| **Lane E**: 前端 | T18 → T19 → T20 → T21-T28(可并行) → T29-T32(测试) | 对应API可用 |

执行顺序：**Lane A 先行 → Lane B + Lane C 并行 → Lane D → Lane E**

冲突标记：Lane B和Lane C无共享模块目录。Lane E前端依赖Lane B/C的API完成。

---

## 十、架构决策记录

| ID | 决策 | 来源 |
|----|------|------|
| AD1 | 后端按功能域物理拆分包结构（employee/, project/, kpi/ 等15个独立包） | 本次审查 |
| AD2 | 通用 CRUD 抽取为 BaseService/BaseController，各模块继承 | 本次审查 |
| AD3 | 配置向导采用分步保存模式，每步独立API，支持断点续配 | 本次审查 |
| AD4 | 测试采用标准覆盖：30个单元测试 + 10个集成测试 + 10个算分参数化测试 + 2个E2E | 本次审查 |
| AD5 | 代码注释强制：Java文件3行头注释 + 每方法一行白话注释；React组件功能注释 + data-area div | 本次审查 |

**已从设计文档继承**：D1-D14（CEO Review全部决定），详见 `design-绩效考核系统-20260518.md` 第六节。

---

## 十一、外部审查修正记录

2026-05-18 Outside Voice (Claude 子代理) 发现5个问题，全部采纳修正：

| # | 问题 | 修正 |
|---|------|------|
| 1 | 配置向导缺少持久化表 | wizard 模块新增 `WizardProgressMapper` + `WizardProgress` 实体 → `wizard_progress` 表(V1__init.sql) |
| 2 | ScoreCalculator 依赖不存在 | kpi 模块新增 `ScoreCalculator.java`（纯函数算分引擎），提前到 Phase 1 |
| 3 | 两条导入路径冲突 | 删除 employee 模块的简单导入，统一使用 import 模块的两步导入 |
| 4 | 缺少种子数据迁移 | 新增 `V3__seed_data.sql`：默认 system_param 值 + 初始 ADMIN 用户 |
| 5 | Redis 对2-3用户过早 | 代码保留 Redis 配置，部署文档标注"Phase 1 可选" |

**无跨模型分歧** — 外部审查所有发现均被主审查认同并修正。

---

## 十二、数据库表清单（阶段1，共12张）

| # | 表名 | 说明 | 所属模块 |
|---|------|------|---------|
| 1 | employee | 员工信息 | employee |
| 2 | user | 系统用户 | user |
| 3 | user_role | 用户角色分配 | user |
| 4 | project_role | 项目角色（动态可配置） | projectrole |
| 5 | project | 项目信息（含乐观锁version） | project |
| 6 | project_role_assignment | 项目角色人员分配（含乐观锁version） | roleassignment |
| 7 | position_assessment_config | 岗位考核配置（含乐观锁version） | position |
| 8 | position_assessor_role_config | 岗位考核人角色关联（关联表，D10） | position |
| 9 | project_kpi_config | 项目KPI指标（含乐观锁version） | kpi |
| 10 | func_kpi_config | 职能KPI指标（含乐观锁version） | kpi |
| 11 | system_param | 系统参数 | system |
| 12 | assessment_period | 考核周期 | period |
| — | wizard_progress | 配置向导进度（✨外部审查新增） | wizard |

> 阶段2新增: employee_project_participation, assessment_task, assessment_score, notification
> 阶段3新增: appeal, audit_log

---

*工程规划完成时间: 2026-05-18 | 范围: 阶段1 配置中心 | 模块数: 15后端 + 12前端 + 4公共组件 | 外部审查: 5/5 修正* | **设计审查: 12/12 决策确认**

---

## 十三、UI设计规范（Design Review 输出）

> 本章由 /plan-design-review 于 2026-05-18 生成。全部12项设计决策已确认。
> 格式说明：每项决策分两部分——**「给非技术人员看的」**用大白话解释用户看到什么、点击什么发生什么；**「给开发人员看的」**保留专业规格。

### 13.1 设计Token速查卡（给开发）

| 类别 | Token | 值 | 用途 |
|------|-------|-----|------|
| **字体** | 中文 | -apple-system, PingFang SC, Microsoft YaHei | 全局中文 |
| | 英文/数字 | Inter, -apple-system | 全局英文和数字 |
| | 标题 | 20px / font-weight:500 | 页面标题 |
| | 正文 | 14px / font-weight:400 | 表格、表单、正文 |
| | 辅助文字 | 12px / font-weight:400 | 提示、标签、次要信息 |
| **色彩** | 主操作 | #1890FF | 按钮、链接、选中态、步骤条当前步 |
| | 成功 | #52C41A | 保存成功横幅、对勾图标、已配置标签 |
| | 警告 | #FAAD14 | 409冲突弹窗、提醒通知 |
| | 危险 | #FF4D4F | 删除按钮、错误提示、校验失败红字 |
| | 中性 | #8C8C8C | 次要文字、禁用态、占位符 |
| | 页面背景 | #F5F5F5 | 页面底色 |
| | 卡片/表格背景 | #FFFFFF | 卡片、表格、弹窗背景 |
| **圆角** | 按钮 | 6px | 所有按钮 |
| | 输入框/标签 | 4px | 输入框、标签、下拉框 |
| | 卡片 | 8px | 卡片、弹窗 |
| **阴影** | 卡片 | 0 1px 2px rgba(0,0,0,0.06) | 卡片浮起感（极淡） |
| | 弹窗 | 0 4px 12px rgba(0,0,0,0.12) | 弹窗层级感 |
| | 其他 | 无阴影 | 表格、按钮、输入框不设阴影 |

**AI痕迹禁止清单（开发自查）：**
- ❌ 紫色/靛蓝渐变背景
- ❌ 图标放在彩色圆圈里的3列等宽布局
- ❌ 装饰性波浪线、漂浮圆球、渐变SVG分割线
- ❌ 全部元素统一大圆角（如全站8px）
- ❌ 卡片左边框彩色竖线（border-left: 3px solid accent）

### 13.2 全局布局

**给非技术人员看：** 整个系统像一个"三明治"结构——左边是导航菜单（深色侧边栏），顶部有系统名称和用户名，中间大面积是操作区域。就像Windows资源管理器：左边选目录，右边看内容。

**侧边栏导航：**

```
┌─────────────┐
│ 🏠 继峰考核  │  ← 系统Logo+名称，点击回仪表盘
│             │
│ 📊 仪表盘   │  ← 当前在哪个页面，哪个就高亮
│             │
│ ── 配置中心 ─│  ← 分组标题（不可点击，纯文字）
│ 🧙 配置向导  │
│ 👥 员工管理  │
│ 🎯 项目角色  │
│ 📁 项目管理  │
│ 🔗 角色分配  │
│ ⚙️  岗位配置  │
│ 📈 KPI配置   │
│ 📅 考核周期  │
│             │
│ ── 系统设置 ─│
│ 👤 用户管理  │
│ 🔧 系统参数  │
│ 📋 直属上级  │
└─────────────┘
```

**技术规格（给开发人员看）：** 侧边栏宽200px（桌面端），深色背景（#001529），白色文字+图标。菜单分3组以分隔线隔开。当前激活菜单项浅色背景高亮。平板端（768-1023px）折叠为60px纯图标模式。

### 13.3 仪表盘页面（/dashboard）

**给非技术人员看：**

> **第一次登录时看到的画面：**
> 页面中央最显眼的是一张大插图（欢迎氛围）+ 标题"欢迎使用绩效考核系统" + 副标题"完成7步配置，预计需要15-20分钟" + 一个大大的蓝色按钮"开始配置"。
> 
> 按钮下方是6张小卡片排成2行×3列，分别写着"员工数据""项目角色""项目管理""角色分配""KPI指标""岗位配置"，每张卡片标注"待配置"灰色标签。
> 
> **点击"开始配置"按钮→** 跳转到7步向导第1步。
> 
> **配置完成后再回来：** 大插图和按钮消失，6张卡片全部变成绿色"已配置"，仪表盘上方显示"配置完成度：100%"。
> 
> **阶段2及以后：** 仪表盘顶部多一个"差异报告提醒"——红色圆点数字显示有多少条异常需要处理。

**技术规格（给开发人员看）：**
- 组件：DashboardPage.jsx
- 首次访问判断：GET /api/v1/dashboard/config-progress 返回所有项为0时显示引导型布局
- 配置进度卡片：2行×3列CSS Grid，gap:16px
- 已配置项卡片：绿色左边框+绿色✓图标
- 差异报告红点：Badge组件，仅count>0时显示

### 13.4 登录页（/login）

**给非技术人员看：**

> 打开系统首先看到的是登录页——浅灰色背景，中央一个白色卡片框。
> 
> 卡片从上到下依次是：系统Logo+"继峰座椅绩效考核系统"→用户名输入框→密码输入框（输入时显示为●●●●）→蓝色"登录"按钮。
> 
> **输入用户名和密码后点击"登录"→** 按钮变灰+显示转圈"登录中…"→成功后自动跳转到仪表盘。
> 
> **输错密码→** 按钮上方出现红色文字"用户名或密码错误"，按钮恢复正常可重新点击。
> 
> **没登录就直接访问/api/*地址→** 自动跳回这个登录页。

**技术规格（给开发人员看）：**
- 组件：LoginPage.jsx
- 布局：居中卡片，宽度400px，圆角8px
- 登录按钮：width:100%, height:40px, #1890FF
- 加载态：Button loading={true} + 文字"登录中…"
- 错误态：红色Alert组件在按钮上方，type="error"
- 密码框：Input.Password 带眼睛切换明文

### 13.5 7步配置向导（/setup-wizard）

**给非技术人员看：**

> 向导页面顶部有一条横着的"进度条"——7个小圆点+步骤名排成一行。
> 
> **怎么看进度：** 已完成的步骤显示绿色对勾✅+步骤名（可点击跳回去修改），正在做的步骤蓝色高亮🔵，还没做的步骤灰色○不可点击。
> 
> **填写表单：** 当前步骤的表单在中间大区域。比如第3步"创建项目"，有4个输入框：项目编码、项目名称、项目阶段（下拉选择）、项目说明。
> 
> **点击"下一步"→** 按钮变灰+转圈+文字变"保存中…"，保存成功后当前步骤变绿对勾✅，自动跳到下一步。
> 
> **表单有没填的必填项→** 没填的输入框下方出现红字提示（如"项目编码不能为空"），不跳步骤。
> 
> **保存失败（网络问题）→** 页面顶部出现红色横幅"保存失败：网络异常，请点击重试"，可点✕关闭。下一步按钮恢复可点击。
> 
> **中途离开（关闭页面或点其他菜单）→** 随时可以走。下次回来时，页面顶部显示蓝底提示"检测到上次配置进度，已自动恢复到第X步"，继续从上次的位置做。
> 
> **第7步完成后点"完成"→** 弹出一个大绿色对勾🎉+"恭喜！配置已完成！"，2秒后自动跳回仪表盘，仪表盘从空状态变成全部"已配置"。

**技术规格（给开发人员看）：**
- 容器组件：SetupWizardPage.jsx
- 7个子步骤组件：StepProjectRole.jsx ~ StepCreatePeriod.jsx
- 步骤条：Ant Design Steps 组件，type="navigation"，当前步current，已完成步status="finish"
- 步骤进度持久化：POST /api/v1/wizard/step/{step-name}，wizard_progress表存储user_id/current_step/completed_steps
- 恢复提示：GET /api/v1/wizard/progress返回currentStep>0时，在页面顶部显示Alert type="info" banner "检测到上次配置进度，已自动恢复到第X步"
- 保存按钮loading态：Button loading={true} disabled，文字从"下一步 →"变为"保存中…"
- 校验失败：表单字段下方Form.Item help属性显示红字错误
- 网络错误：页面顶部Alert type="error" banner + closable
- 完成弹窗：Modal success类型，2秒setTimeout后router.push('/dashboard')

### 13.6 数据列表页通用布局

**给非技术人员看：**

> 所有"列表页"（员工管理、项目管理、岗位配置等）布局统一，从上到下：
> 
> ① **页面标题栏**：左边是页面名称（如"员工管理"），右边是蓝色"新增"按钮。点击新增按钮弹出表单弹窗。
> 
> ② **搜索筛选栏**（浅灰背景条）：搜索框可输入姓名/工号模糊搜索，后面跟岗位下拉框和状态下拉框，最右边"搜索"按钮。点击搜索后表格数据刷新。
> 
> ③ **数据表格**：隔行浅灰背景（斑马纹），最后两列是"编辑"和"删除"按钮。
> 
> ④ **底部分页器**：显示"第1页/共25页"，左右箭头翻页，可点页码跳转。每页默认20条。
> 
> **点击编辑按钮→** 弹出编辑弹窗，表单自动填入该行当前数据，修改后点确定保存。如果保存时提示"数据已被他人修改"（409冲突），说明有人同时在编辑同一条，点"刷新页面"重新来。
> 
> **点击删除按钮→** 弹出红色确认弹窗（"确定要删除XXX吗？此操作不可撤销。"），点"确认删除"后该行从表格中淡出消失，顶部绿色横幅"已删除"闪现3秒后自动消失。

**技术规格（给开发人员看）：**
- 布局组件：PageHeader + 搜索筛选栏 + Table + Pagination
- 分页：默认pageSize=20, showSizeChanger=true, pageSizeOptions=[10,20,50]
- 表格：Ant Design Table, rowKey=id, bordered=false, size="middle"
- 斑马纹：rowClassName按index奇偶设置
- 新增弹窗：Modal title="新增XXX", 表单初始值为空
- 编辑弹窗：Modal title="编辑XXX", 表单initialValues从GET :id获取
- 删除确认：Modal.confirm, okType="danger", okText="确认删除"
- 409冲突弹窗：Modal.warning, title="数据已被他人修改", content="XXX在X秒前修改了这条记录。请刷新页面后重新编辑。", okText="刷新页面", onOk=window.location.reload()
- 保存成功反馈：message.success("保存成功", 3) 顶部绿色横幅
- 删除成功反馈：message.success("已删除", 3)

### 13.7 空状态设计

**给非技术人员看：**

> 首次使用时所有列表页都是空的——没有员工、没有项目、没有KPI。每个空页面不会只显示干巴巴的"暂无数据"四个字，而是有：
> - 一个应景的插图（如员工页是空文件夹插画，项目页是空项目图标）
> - 标题文字说明当前状态（如"还没有任何员工数据"）
> - 引导文字告诉你可以做什么（如"导入继峰现有员工Excel或手动新增"）
> - 1-2个行动按钮引导下一步操作

**各页面空状态规格：**

| 页面 | 插画类型 | 标题 | 引导文案 | 按钮1 | 按钮2 |
|------|---------|------|---------|-------|-------|
| 员工管理 | 空文件夹 | 还没有任何员工数据 | 导入继峰现有员工Excel或手动新增 | 📥 批量导入Excel | ➕ 手动新增 |
| 项目管理 | 空项目 | 还没有任何项目 | 请先完成项目角色配置，再创建项目 | 🎯 先去配置项目角色 | — |
| 项目角色 | 空角色 | 还没有任何项目角色 | 先创建角色（如PDL/PQL/Launch），后续可随时增删 | ➕ 新增角色 | — |
| 岗位配置 | 空表单 | 还没有任何岗位配置 | 为每个岗位设置考核权重和考核人角色 | ➕ 新增岗位配置 | — |
| KPI配置 | 空图表 | 还没有任何KPI指标 | 项目KPI按角色+阶段组合配置，职能KPI按岗位+分类组合配置 | 📈 配置项目KPI | 📋 配置职能KPI |
| 考核周期 | 空日历 | 还没有任何考核周期 | 创建第一个考核周期后即可开始考核 | ➕ 创建考核周期 | — |
| 用户管理 | 空用户 | 还没有任何系统用户 | 为需要使用系统的人创建登录账号 | ➕ 新增用户 | — |
| 系统参数 | 空设置 | 还没有自定义参数 | 所有参数使用系统默认值，如需修改可在此页面操作 | 查看默认值 | — |

**技术规格（给开发人员看）：**
- 组件：EmptyState.jsx（Props: image, title, description, primaryAction?, secondaryAction?）
- 空状态判断：dataSource.length === 0 && !loading
- 插画：使用Ant Design Empty组件或自定义SVG插画
- 主按钮：type="primary"，次按钮：type="default"
- 仪表盘引导型空状态用独立的WelcomeCard组件，不用EmptyState

### 13.8 操作反馈规范

**给非技术人员看：** 系统中任何操作都有明确的视觉反馈——成功了告诉你成功了、失败了告诉你原因和怎么解决。不会有"按钮按了没反应"的情况。

| 操作 | 用户看到什么 | 持续时间 |
|------|------------|---------|
| 新增/编辑保存成功 | 页面顶部滑入绿色横幅"保存成功" | 3秒自动消失 |
| 删除成功 | 被删行淡出消失+绿色横幅"已删除" | 3秒自动消失 |
| 乐观锁冲突(409) | 黄色弹窗"数据已被他人修改。XXX在X秒前修改了这条记录。" + 蓝色"刷新页面"按钮 | 用户手动关闭 |
| 删除确认 | 红色弹窗"确定要删除XXX吗？此操作不可撤销。" + 灰色"取消"+红色"确认删除" | 用户手动选择 |
| 表单校验失败 | 出错的输入框下方红色小字提示具体原因 | 直到用户修正 |
| 网络请求失败 | 红色横幅"网络异常，请点击重试" | 用户手动关闭 |
| 未登录访问 | 自动跳回登录页 | — |
| 无权限操作(403) | 页面显示"无权访问"提示 | — |

**技术规格（给开发人员看）：**
- 成功反馈：message.success(msg, duration)
- 错误反馈：message.error(msg, duration)
- 409冲突：Modal.warning + onOk刷新
- 删除确认：Modal.confirm + okType="danger"
- 表单校验：Form.Item rules + help属性显示
- 所有弹窗按钮文案不使用"OK/Cancel"英文，使用中文（"确认删除/取消""刷新页面""我知道了"）

### 13.9 岗位配置 & KPI配置页特殊布局

**给非技术人员看：**

> **岗位配置页**：左边是配置列表（表格），右边是编辑面板。点击左边某一行，右边自动显示这条配置的详细编辑界面。
> - 权重用"滑块+数字"表示（如项目权重70%、职能权重30%），拖滑块数字跟着变。两个权重加起来必须是100%，不正确时显示红色提示。
> - 考核人角色用勾选框（☑ PDL ☑ PQL ☐ Launch），勾了谁谁来考核。
> - 最下面有一个开关"纳入项目制"，蓝色=纳入。
> 
> **KPI配置页**：顶部是筛选条件（选择角色+选择阶段），下面的表格显示该组合下的所有KPI指标。
> - 权重可以点数字直接改，改完按回车保存。所有权重加起来如果不是100%，表格底部红色提示"权重之和≠100%"。
> - 最后一列是操作按钮（编辑/停用）。停用的指标不显示在后续考核中。

**技术规格（给开发人员看）：**
- 岗位配置：左侧Table(60%宽) + 右侧Card(40%宽)，选中行高亮，右侧根据selectedRow渲染编辑表单
- 权重滑块：Ant Design Slider, min=0, max=100, step=1, 实时计算对侧权重=100-当前值
- 权重校验：两值之和不等于100时Form.Item validateStatus="error"
- 考核人角色：Checkbox.Group, options从GET /api/v1/project-roles?isActive=true获取
- 纳入开关：Switch checkedChildren="是" unCheckedChildren="否"
- KPI配置：顶部Space横向排列筛选条件，Table columns中权重列使用InputNumber内联编辑(onBlur触发保存, onPressEnter确认)
- 权重组和校验：表格底部Summary行显示总和，不等于100时红色高亮

### 13.10 响应式断点策略

| 断点 | 宽度 | 行为 |
|------|------|------|
| 桌面端 | ≥1024px | 完整侧边栏(200px) + 内容区 |
| 平板端 | 768-1023px | 侧边栏折叠为图标(60px)，表格水平滚动 |
| 手机端 | <768px | 居中显示"请在电脑上访问以获得完整体验" |

### 13.11 无障碍检查清单（开发自检）

- [ ] 所有按钮和链接可通过Tab键到达，Enter/Space可触发
- [ ] 所有表单输入框关联`<label>`元素（禁止只用placeholder做标签）
- [ ] 表单校验错误提示关联`aria-describedby`
- [ ] 弹窗打开时焦点自动移入第一个可聚焦元素，关闭时焦点回到触发按钮
- [ ] 表格`<th>`有`scope`属性（屏幕阅读器方向感知）
- [ ] 步骤条当前步标记`aria-current="step"`
- [ ] 通知横幅使用`role="alert"`（自动播报给屏幕阅读器）
- [ ] 颜色对比度 ≥4.5:1（文字#262626 vs 白色背景 = 16:1 ✅）
- [ ] visited链接与未访问链接颜色有区分

### 13.12 用户旅程故事板（HR第一次使用）

```
场景1：登录 → 😐→😊
小王输入admin/admin123，点击"登录"
→ 按钮变灰+转圈"登录中…"→跳转仪表盘
→ 看到"欢迎使用绩效考核系统"+蓝色"开始配置"按钮
小王心想："哦，原来要7步配置，看起来不难"

场景2：向导第1-3步 → 😊→😎
→ 步骤1：创建3个项目角色(PDL/PQL/Launch)，轻松完成
→ 步骤2：上传Excel导入200名员工，看到预览正确
→ 步骤3：创建第一个项目，填写表单完成
小王心想："挺顺利的，已经3/7了"

场景3：向导第4-5步（疲劳拐点） → 😎→😐→😊
→ 步骤4：分配角色人员，需要仔细核对
→ 步骤5：配置KPI指标，权重必须100%，稍复杂
小王有点累，看到页面顶部"已自动保存，可随时离开"
→ 离开去喝水，回来看到"检测到上次进度，已恢复到第5步"
小王心想："还好没丢，继续做完吧"

场景4：向导第6-7步+完成 → 😊→🎉
→ 步骤6-7：配置岗位→创建周期
→ 点击"完成"→绿色大对勾🎉"恭喜！配置已完成！"
→ 2秒后自动跳回仪表盘→6张卡片全部绿色"已配置"
小王心想："搞定了！可以开始考核了"

场景5：日常使用 → 😐(平静高效)
→ 每周打开→仪表盘显示配置状态
→ 员工离职→去列表搜索→编辑→保存→绿色横幅
→ 新增项目→创建→分配角色→一切顺畅
→ 小王习惯了系统，不再需要向导
```

---

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 1 | CLEAR | mode: HOLD SCOPE, 0 critical gaps, 14 decisions closed |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEAR | 5 issues found, 0 critical gaps, all resolved |
| Outside Voice | `/codex-plan-review` | Independent 2nd opinion | 1 | DONE | 5 issues found (Claude subagent), all 5 accepted and fixed |
| Design Review | `/plan-design-review` | UI/UX gaps | 1 | CLEAR | score: 2/10 → 9/10, 12 decisions made |

- **UNRESOLVED:** 0
- **VERDICT:** CEO + ENG + DESIGN CLEARED — ready to implement Phase 1
